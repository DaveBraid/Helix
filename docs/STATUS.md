# 当前开发状态

最后更新：2026-08-20
当前基线提交：`9e6b417 feat: unify task detail experience`（分支 `dev`）
工作树状态：仅架构、状态与设计验收文档尚未提交；业务代码工作树干净。
当前阶段：统一任务详情页与 Task Reference 退役已形成阶段提交；暂不发布、不合并主分支。

## 本轮目标

- 将普通滴答任务、Stage 计划行动和 Stage 投影父任务收敛为一套详情页，并严格隔离各自权威写入适配器。
- 非目标：不执行真实滴答写入、不改变项目同步合同、不发布版本。

## 当前事实

- Project／Stage Markdown 与专用 Canvas 是项目权威源；`data.json` 只保存恢复、同步与事件状态。
- 进行中 Stage 对应 `Helix Projects` 远端父任务；`# 计划行动`对应真实 `parentId` 子任务。
- 本地计划行动与子任务直接从 Stage Markdown 投影到任务页，本地编辑使用 Stage revision CAS。
- 任务页按真实父子关系缩进，完成项同级后置，并可隐藏已完成。
- 项目工作区派生缓存仅供渲染；失效后由 Markdown／Canvas 重建，渲染不得补写文件。
- 普通滴答同步、项目同步、离线队列、写门和冲突合并共用 `HelixService` 管线。
- 版本为 `1.0.1`，最低 Obsidian `1.12.2`。

## 本轮改动

- 新增 `TaskDetailDraft`、`TaskDetailCapabilities`、`TaskDetailAdapter` 与唯一 `UnifiedTaskDetailModal`。
- 三种来源共用标题、属性矩阵、日历／时间弹层、标签、子任务进度／列表和操作区；来源只显示在面包屑与同步状态。
- 普通滴答任务经既有同步队列保存；Stage 行动经 Markdown CAS；Stage 父任务先改 Markdown，再由后台同步滴答。
- “更多属性”只保留清单、提醒、重复；移除备注大区、时区文字、重复铅笔和两套旧详情实现。
- Task Reference 运行时、编辑器、诊断和创建入口已删除；用户原有引用 Markdown 原样保留且不再读取或改写。
- 统一详情页已按设计稿调整宽度、标题边框、明暗主题和窄窗口布局。

## 相关约束

- 权威源、同步写门与任务详情适配见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 远端结果未知禁止重发；冲突禁止静默覆盖；保存失败不得关闭草稿。
- 凭证只存 SecretStorage；真实写入只使用精确 ID 与 Helix 专用对象。
- 不支持的能力必须禁用或隐藏，不得用界面成功替代已验证合同。

## 当前验证

- 完整门禁通过：71 个测试文件、998 项测试；`typecheck`、`build`、`release:check`、`git diff --check` 全部通过。
- 针对性测试：任务详情领域与工作台结构 44 项通过。
- Obsidian CLI 实机：Stage 投影显示 Markdown 权威状态和 3 个计划行动；普通滴答来源显示同步清单与“更多属性”；未点击保存、删除或完成。
- 视觉验收：1600 × 960 同屏比对、720 × 960 窄窗口无横向溢出、亮色与暗色主题均通过，证据见根目录 [design-qa.md](../design-qa.md)。

## 未关闭问题

- 普通滴答子任务新增／删除／排序仍受远端合同能力限制；当前明确禁用，不伪写。
- Stage 父任务的根计划行动尚无独立排序服务，当前不开放拖拽；Stage 行动内部子任务可排序。
- 大量任务的整页 DOM 重绘约 60–103 ms；如真实使用仍卡顿再评估虚拟化。
- 429 沿用持久请求治理；不得主动触发限流验证。

## 下一步

1. 跑完整测试、构建、发布检查和 diff 检查，修复任何回归。
2. 重载插件并确认无控制台错误，恢复 CLI 临时视口设置。
3. 自审适配器权威边界与失败保稿路径，形成阶段提交。
4. 用户实测三种来源的编辑体验后，再补已验证的子任务远端能力。
