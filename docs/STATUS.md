# 当前开发状态

最后更新：2026-08-06
当前基线提交：`f793f8d fix: unblock local project workflows`
工作树状态：工作树干净；Vault 测试数据和运行态 `data.json` 不纳入 Git。
当前阶段：项目页本地操作阻塞已修复并通过实机验收，下一轮继续打磨项目核心体验。

## 本轮目标

- 修复 5 条伪聚焦冲突、加入同步后的隐藏事务写入失败，统一用户可见术语并收紧 fallback 路径。
- 非目标：不运行新写入合同，不启用生产滴答项目同步，不写入或清理普通滴答数据，不发布 Release。

## 当前事实

- 普通任务双向同步、滴答清单列表／看板呈现及专注时长读取可用；滴答仍是任务、清单、习惯和专注记录的权威源。
- Project／Stage Markdown 保存项目身份、状态与正文，Canvas 保存继承／分支／合并关系和布局；项目页只投影 Obsidian 原生数据。
- 复盘支持日／周／月／年模板、创建和回看；分析与挑战只消费事件账本。
- 项目向滴答目前仅为默认关闭的显式单向同步；生产清单／分栏尚未配置，本轮没有启用或验证。
- 合同版本 v6；任务 CRUD、提醒、重复、父子、重开、看板归栏和分栏创建能力分别验证并绑定授权。
- 4D 安全清理和 Electron 二次确认计时器修复已提交；远端结果未知仍禁止重发。
- 凭证只存 Obsidian SecretStorage；清理计划、界面和普通诊断不得披露口令、临时对象 ID 或唯一标记。

## 本轮改动

- `src/domain/stage-focus-bridge.ts`：将 Obsidian 等价的 `[[path]]`／`[[path.md]]` 识别为同一 canonical 来源；仅放宽扩展名，正文、标题、身份和其他结构变化仍冻结。
- `src/storage/vault-repository.ts`、`src/main.ts`：构造时注入当前 root 下三条精确事务路径，执行双读 fence 和写后复读；其他路径及普通 Markdown／Canvas 禁止 adapter fallback。
- `src/ui/` 与相关服务异常：统一同步术语；Canvas 连线改为“交由 Helix 管理”，聚焦修复改为“重建自动引用”，并明确只有“同步此项目到滴答”请求远端。
- `tests/`：覆盖等价 WikiLink、精确事务路径 CAS、非目标根拒绝、普通 Markdown 禁止降级及 UI 术语边界。

## 相关约束

- 权威源、合同与远端门规则见 [ARCHITECTURE.md](ARCHITECTURE.md)，阶段边界见 [PLAN.md](plans/04-dida-project-projection/PLAN.md)。
- 用户既有滴答任务、清单、分栏、配置和 Obsidian 内容不得用于破坏性测试；本轮清理只处理经过严格身份领养的专用合同对象。
- 远端结果未知、身份不符、双源不一致、对象竞争或证据不足必须冻结；禁止猜测、静默覆盖或重复发送非幂等写入。
- 后续开发继续复用任务日期、提醒、重复、看板与同步领域逻辑，不为项目或复盘视图建立冗余实现。

## 当前验证

- 已通过：全量 63 个测试文件、784 项；`typecheck`、`build`、`release:check`、`git diff --check`。
- 已通过实机：CLI reload 后聚焦冲突 `5→0`、checkpoint `0→5`，8 份项目 Markdown hash 均未变化。
- 已通过实机：行动“加入同步”及状态往返、临时 Stage 加号创建／删除；queue／conflicts／receipts 均为 0，开发错误与 console error 均为 0。
- 已通过：4D 专用合同残留已精确清理；本地 pending／recovery 为 0，只读远端残留为 0。
- 未运行新的写入合同；未触碰普通滴答数据或 Obsidian 用户数据。
- 不得据此宣称生产滴答项目同步、生产分栏创建或 Release 已通过。

## 未关闭问题

- 当前滴答 API 授权存在且读取连接正常，但 4D 清理后生产写合同能力已失效；这不是纯本地项目操作的阻塞，远端项目同步必须保持禁用并明确提示重新验证。
- 项目页与复盘体验仍是下一阶段核心，尚未达到最终产品质量。
- 生产滴答项目同步仍保持默认关闭且未配置；未来启用必须另行显式预览、确认和验收。

## 下一步

1. 继续验收 Canvas 容器、关系编辑、折叠、自动布局与动画聚焦的完整工作流。
2. 梳理 Stage 计划行动与滴答项目同步的启用配置；在写合同重新验证前保持远端写入禁用。
3. 项目核心体验稳定后，进入复盘模板、交叉引用与周期回看。
