# 当前开发状态

最后更新：2026-08-22
当前基线提交：`d2f86bb docs: add recurring development lessons`
工作树状态：工作树干净。
当前阶段：已建立长期开发经验入口；等待下一项 `dev` 开发任务。

## 本轮目标

- 建立精简、可持续维护的高复发开发经验文档，并纳入每次接手必读流程。
- 非目标：不修改业务代码、不运行真实滴答合同、不修改正式 Vault、不发布新版本。

## 当前事实

- Project／Stage Markdown 是项目身份、状态、正文与本地任务的权威源；专用 Canvas 是关系和布局的权威源。
- `2.1.1` 是当前正式版；日常开发只在 `dev`，只有用户明确要求发布时才允许合入 `main`。
- Stage `# 计划行动`进入任务页并形成本地任务树；父任务完成状态由直属子任务派生。
- 含顶层行动的 Stage 都会按 Markdown 状态生成本地父任务；远端父任务存在时复用其精确 ID 和 `Helix Projects` 清单，未绑定时使用本地稳定 ID。
- 滴答 API 口令只建立读取连接；生产写入还要求当前 Vault 中保存的合同能力缓存与当前授权绑定、合同版本一致。
- 正常设置区现直接展示“启用滴答写入”或“滴答写入已就绪”；开发测试折叠区复用同一合同运行入口。
- 自动同步开启但合同能力未完成时只允许读取，并显示远端写入保持只读的明确通知。
- 自动同步在测试 Vault 中仍为关闭状态；本轮没有访问任何滴答远端。
- `docs/经验.md` 只记录会影响安全、验证有效性或效率的高复发经验；每次工作必须读取并按需维护。

## 本轮改动

- 已提交至 `d2f86bb`；新增 `docs/经验.md`，收录容器外 Obsidian CLI、最小化 Computer Use、测试数据边界、布局验收基准与发布纪律。
- `AGENTS.md` 已把 `docs/经验.md` 纳入文档导航和每次工作的固定必读流程。

## 相关约束

- 权威边界、本地任务投影与滴答合同见 [ARCHITECTURE.md](ARCHITECTURE.md) 第 3、5 节。
- 写入验证仍只操作唯一标记的专用临时对象；结果未知或清理未证实时保持只读，禁止触碰既有滴答数据。
- 本地 Stage 父任务只是任务页即时投影，不得写入 `data.json`，也不得伪造远端 ID 或 `Helix Projects` 归属。
- API 口令只存 SecretStorage；能力缓存只保存不可逆授权绑定。换 Vault、换授权或合同版本变化均需重新验证。
- 用户 Markdown／Canvas 禁止静默覆盖；本轮正式 Vault 仅做过只读诊断，没有写入。

## 当前验证

- 已通过完整自动化门禁：71 个测试文件、1023 项；`typecheck`、`build`、`git diff --check` 均通过。
- 针对性测试已覆盖：无远端父任务时生成本地 Stage 父任务、采用 `active` 状态、根行动正确挂接；有远端父任务时保持精确 ID 与清单归属。
- 设置结构测试已覆盖：正常设置区存在写入启用引导、API 读取与写入验证说明、自动同步只读通知，开发折叠区仍存在且复用合同入口。
- Obsidian 1.13.7 实机验收通过：正常设置区显示“滴答写入已就绪”；任务页显示本地 Stage 父任务及“进行中”状态，完成圆保持派生只读；“进行中”筛选只保留对应 Stage 父任务。
- 测试 Vault 的 CLI 开关已重新开启，但当前外部 CLI 进程仍无法连接应用；本轮实机验收改用最少量 Computer Use 完成。应用重启时曾恢复正式 Vault 的既有 Stage-04 标签，随即切回测试 Vault，期间未操作正文、设置、合同或同步入口，未产生正式 Vault 写入。
- 本轮没有运行合同测试、没有联网访问滴答、没有修改正式 Vault。
- 本轮仅修改文档；`git diff --check` 通过，`docs/经验.md` 为 2156 字节，未重复运行代码门禁。

## 未关闭问题

- 测试 Vault 已有有效合同能力；“未验证”引导态由自动化覆盖，实机验证“已就绪”分支。
- 正式 Vault 的写入能力缓存尚未建立；由用户自行运行设置中的写入验证，本轮不代操作。
- Obsidian CLI 开关已经开启但外部命令仍暂时无法连接，后续需单独排查应用端点。

## 下一步

1. 后续每次接手先读取 `AGENTS.md`、本快照和 `docs/经验.md`。
2. 遇到新的高复发问题时精简更新 `docs/经验.md`，不记录一次性流水账。
3. 用户在正式 Vault 自行完成写入验证后，复查 Stage04 是否进入 `Helix Projects`；若远端仍异常，再只读诊断投影链路。
