# 当前开发状态

最后更新：2026-08-10
当前基线提交：`95a876e feat: refine project cards and local task editor`
工作树状态：仅本状态快照待提交；业务代码与测试已提交。
当前阶段：阶段卡片与任务详情完成首轮正式版视觉收口；项目滴答投影保持关闭。

## 本轮目标

- 继续把本地“项目—阶段—任务—子任务”体验打磨到可日常使用，并保持 Markdown／Canvas 为唯一真值。
- 非目标：不恢复项目滴答投影，不访问或写入滴答远端，不修改正式 Vault，不发布版本。

## 当前事实

- Project／Stage Markdown 保存项目、阶段身份、状态和正文；专用 Canvas 保存继承／分支／合并及布局；`data.json` 不保存项目影子真值。
- Stage `# 计划行动` 的顶层清单项自动进入任务页，缩进项作为子任务；首次读取会静默补稳定身份，空模板占位保持原样。
- 本地任务详情采用 Dense task canvas：面包屑、大标题、五项属性矩阵、子任务进度与列表；不显示备注区或时区文字。
- 本地任务时间支持无时间、单时间点和同日时间段；未改动日程时保持原始时间与时区，避免无意归一化。
- 子任务支持完成、标题、日期、时间段、优先级、添加、删除和拖拽排序；保存使用同一 Stage revision/CAS，排序和元数据直接写回原 Markdown 标记，不建立第二份真值。
- 阶段卡片主体保持中性；同项目只用项目色顶部细条、边框和轻阴影；五状态只用图标、文字和语义色。
- 阶段卡片悬停、键盘聚焦或选中时，以同尺寸操作栏替换关系摘要，提供新增、连接、折叠、删除；现有关系拖拽、连线和画布相机逻辑保留。
- 聚焦桥接以 Canvas 入边为关系源，支持继承、分支、合并、反向编辑及同窗竞争冲突。
- 冲突中心按“需要你选择／等待远端核对／仅需检查”分组；文本使用真实行号 IDE diff，标量属性按需展开。
- 当前版本不展示项目滴答投影入口；统一门禁覆盖后台扫描、队列、恢复、重试和冲突写回。
- 凭证只存 SecretStorage；版本 `0.1.6`，最低 Obsidian 桌面端 `1.12.2`。

## 本轮改动

- `project-lineage-workbench.ts`、`styles.css`：重排卡片层级与项目色，加入四操作栏，尺寸调整为 292 × 144。
- `helix-view.ts`：重做本地任务详情；加入自适应日程、进度环、子任务更多菜单和拖拽交互。
- `local-project-tasks.ts`、`dida-project-projection.ts`：在单次 Markdown CAS 中保存子任务元数据和显式顺序，逐字保留现有任务标记内容。
- 三组测试更新几何合同、界面结构、子任务排序与元数据；`design-qa.md` 保存两张源图、实现截图和并排证据。

## 相关约束

- 权威源、Stage 本地任务及项目投影边界见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 项目写入必须串行并使用 revision／CAS；不得覆盖未知正文或保存第二份本地任务真值。
- 本地子任务重排只允许同一父任务的直接子任务身份全集；身份缺失、重复或集合变化必须拒绝。
- 远端结果未知时禁止重试；同字段竞争必须逐字段人工选边。
- 真实滴答测试只可使用唯一合同标记和专用清单；不得删除、移动或覆盖用户已有远端对象。

## 当前验证

- 完整门禁：69 个测试文件、954 项测试，以及 `typecheck`、`build`、`release:check`、`git diff --check` 全部通过。
- Obsidian CLI：插件可重载；阶段卡片悬停时操作栏 opacity=1、关系摘要 opacity=0；任务详情存在 5 个属性单元、无备注和时区输入，时间段与 50% 进度状态可切换。
- 视觉 QA：项目卡片和任务详情均完成源图／实现图并排比较；`design-qa.md` 结果为 `passed`。
- 本轮未访问滴答远端；测试 Vault 滴答缓存仍为空，`autoSync=false`，API 口令保留。
- 测试 Vault 当前仍显示一条既有 Canvas 节点身份重复／不一致的聚焦桥接警告；本轮未把它误归因于卡片或任务编辑器。

## 未关闭问题

- 测试 Vault 项目 Canvas 存在节点身份不一致或重复警告，需要下一轮在纯本地范围修复并回归静默整理。
- 本地任务暂不支持跨 Stage／项目移动；需要多文件事务后再开放。
- 滴答 `items` 写入结果未知，项目滴答投影继续关闭。
- 2026-08-05 孤儿合同任务远端状态仍不确定，禁止盲目清理。

## 下一步

1. 修复测试 Vault 的 Canvas 节点身份警告，验证项目页静默恢复且不改用户正文。
2. 用含继承、分支、合并和五状态的本地项目夹具复测新版卡片、折叠、连接及自动排布。
3. 实机保存一组无时间／时间点／时间段与子任务重排，核对 Stage Markdown 逐字边界和重载一致性。
4. 继续打磨复盘页；本地项目与复盘达到正式版质量后再恢复滴答合同研究。
