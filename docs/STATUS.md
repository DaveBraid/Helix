# 当前开发状态

最后更新：2026-08-13
当前基线提交：`bb974ce feat: verify project sync in Dida contract v10`（分支 `dev`）
工作树状态：仅本状态快照待提交；业务代码工作树干净。
当前阶段：普通任务双向同步已验证；正在以唯一临时对象验证项目单向同步，发布门禁保持关闭。

## 本轮目标

- 在不触碰用户既有滴答对象和正式 Vault 的前提下，依次验证只读、专用写入合同、普通任务同步、恢复机制及项目单向同步。
- 非目标：本阶段不发布版本；真实项目同步合同和生产队列实测完成前，不开放项目同步入口。

## 当前事实

- Project／Stage Markdown 是项目内容权威源，专用 Canvas 保存关系及布局；`data.json` 只保存恢复与同步状态。
- Stage `# 计划行动` 顶层条目进入 Helix 任务页；缩进条目为本地子任务。
- 滴答能力分为只读、普通任务写入、专用合同和项目同步四层；前三层代码开放，项目同步由独立发布门禁硬关闭。
- 普通任务 CRUD、跨清单移动、完成／重开、逐字段冲突、重启队列、后台同步和真实父子关系已有自动化与真实合同证据。
- 项目父级映射为带稳定 marker 的普通滴答 Task；Stage 行动映射为带稳定 `parentId` 和 marker 的普通子 Task，不使用父任务 `items`。
- v10 合同新增 `projectProjectionVerified`：在同一唯一测试清单和持久清理计划内复用正式项目同步编排器，验证父子任务创建、属性更新、完成、重开与删除。
- 远端结果未知禁止重发；同字段竞争保留 Base／Local／Remote 并逐字段人工选择。
- 版本 `1.0.1`，最低 Obsidian 桌面端 `1.12.2`；本阶段不发布。

## 本轮改动

- 合同探针版本升至 v10，主体预算由 140 提高到 180，安全清理仍独立保留 40 次；新增项目同步闭环能力位。
- 新增内存 Markdown／状态端口与合同受控远端管线，所有临时父子任务先进入外层持久清理计划，再允许继续验证。
- 项目行动属性更新与完成／重开拆成独立意图，状态变化调用正式 `completeTask`／`reopenTask`，不再误用普通更新载荷。
- 日期复读按公开接口的秒级时间语义比较；失败诊断只报告字段名，不泄露对象内容或身份。
- 旧 items 队列、冲突和账本继续永久只读隔离；项目同步发布门禁仍为关闭。

## 相关约束

- 权威源、能力缓存、合同清理、请求治理和项目同步复用规则见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 真实滴答测试只能创建本轮唯一标记对象；删除前必须精确核对 ID、名称、归属与集合，结果未知时只读复核、绝不重发。
- 凭证只存 SecretStorage；不得写入文档、日志、快照或测试输出。
- 正式 Vault 与用户既有滴答任务、清单和配置均不在测试授权范围内。

## 当前验证

- 真实 v9 合同已完整通过：122 次主体调用，限流计数未增加，队列／冲突／恢复问题／清理票据均为零；全部临时对象已证明清除。
- v10 针对性门禁：6 个测试文件、277 项通过；覆盖成功闭环、未知创建隔离、能力缓存与发布门禁。
- 最新完整门禁：70 个测试文件、946 项全部通过。
- `typecheck`、`build`、`release:check`、`git diff --check` 已通过。
- v10 真实合同与生产队列项目同步尚未验证，不得宣称项目同步可用。

## 未关闭问题

- v10 项目同步合同尚未在真实滴答专用临时对象上运行。
- 项目同步的生产离线队列、冲突、限流与结果未知恢复仍需专用实测；发布门禁保持关闭。
- 本地任务暂不支持跨 Stage／项目移动；提醒、重复和检查项能力保持只读。

## 下一步

1. 复跑完整门禁，提交 v10 自动化合同阶段。
2. 通过 Obsidian CLI 重载测试 Vault，仅用唯一临时对象运行真实 v10 合同并证明零残留。
3. 用隔离的开发入口验证生产离线队列、冲突、限流与结果未知恢复，禁止触碰既有对象。
4. 全部通过后再评估解除项目同步发布门禁；正式发布另行确认。
