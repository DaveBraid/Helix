# 当前开发状态

最后更新：2026-08-04
当前基线提交：`a18ae33 feat: add project and stage lifecycle metadata`
工作树状态：仅本状态快照待提交；Vault 模板、项目文件、Canvas 和运行态 `data.json` 均不纳入 Git。
当前阶段：阶段 1 已提交；准备实施五列阶段看板与状态视觉（阶段 2）。

## 本轮目标

- 落实项目／阶段五态、Markdown 必要属性、稳定阶段编号和只读 Canvas 修复诊断。
- 非目标：本阶段不做看板拖拽、聚焦问题引用块、项目—滴答投影、远端发布或用户数据迁移。

## 当前事实

- 滴答清单是任务、清单、习惯和专注记录的权威源；Project/Stage Markdown 保存项目身份、状态与正文，专用 Canvas 只保存阶段关系和布局，见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 滴答写入合同版本为 4；旧合同、授权不符、结果未知或身份不明均冻结为只读，禁止盲目重试。真实合同临时对象已精确清理，当前阶段不扩展滴答写入。
- 工作台导航为今日、项目、任务、复盘、挑战、冲突；侧栏固定，主内容独立滚动；复盘已合并原分析页。
- 六份默认模板位于可配置 Vault 目录（当前 `Template/Helix/`）；只补缺失文件，绝不覆盖现有模板。创建 Project/Stage 只消费模板正文并生成必要受管属性。
- 项目状态为计划中、进行中、完成、暂停、终止；阶段状态为想法、进行中、完成、暂停、终止。旧 `archived/planned/closed` 仅兼容读取，不批量回写。
- 阶段编号写入 `helix-stage-code`：分支使用 `N.1/N.2`，继承与合并推进大号，历史编号不复用；继承转分支是唯一受控改码例外并同步受管 H1。
- 活动 Project/Stage 笔记通过公开状态栏和命令编辑状态；项目页状态、关系、布局、折叠、颜色、撤销/重做及修复均经过恢复模式写入闸门。
- 项目页纯读取仅报告 `canvasRepairRequired/reasons`，绝不写 Canvas；显式修复使用稳定双快照、受管 Markdown 最终复核和 Canvas CAS，竞争时零写入。

## 本轮改动

- 新增状态规范化、阶段编号领域模块、Project/Stage 必要属性和用户当前模板的默认种子。
- 完成分支／合并编号、删除不复用、跨项目编号、继承转分支 H1/属性事务及回滚测试。
- 新增活动笔记状态入口、项目页五态展示、Canvas 只读诊断和显式修复入口；诊断卡与内嵌画板使用独立宿主，可同时显示。
- 阶段 1 计划已标记完成；阶段 2 计划已注明五列固定单行、列内纵排、窄窗仅看板横向滚动。

## 相关约束

- 凭证仅存 SecretStorage；不得进入仓库、`data.json`、日志、Markdown、Notice 或夹具。
- 用户 Markdown/Canvas 优先；只修改明确受管字段、块、节点和边，写前必须复核修订。冲突不得静默覆盖。
- 恢复模式下项目页可读，但所有写入口必须拒绝；纯读取不得顺手修复派生数据。
- 真实滴答测试仅限唯一标记的专用对象；未知结果、残留或清理身份不明立即停止。

## 当前验证

- 已通过：专项 6 个测试文件、129 项；最终全量 56 个测试文件、571 项；`typecheck`、`build`、`release:check`、`git diff --check` 全绿。
- 已通过：编号、CAS 竞争、回滚、缺失 Canvas 单次创建失败零残留、Markdown 竞争零 Canvas 写、恢复模式零写及诊断/画板同屏回归。
- 实机：Obsidian 1.13.4 CLI 中 Project/Stage 状态栏文案、button 语义和键盘焦点正确；阶段看板显示五态列。
- 实机：项目页同时显示 Canvas 修复原因、显式修复按钮和内嵌画板；未点击修复。重载与切换后 Canvas SHA-256 与验收前一致，Project/Stage 模板及 Stage 文件哈希不变，`dev:errors` 为 0。
- 审查：看门狗最终确认 P0/P1/P2 均为 0；阶段 1 已提交为 `a18ae33`。

## 未关闭问题

- 真正首次安装的模板目录弹窗仅有自动化覆盖；未伪造当前安装状态。
- `Helix/Projects/Test1/Project.md` 于 2026-08-04 19:57 并行改为 `completed`；未恢复，需用户确认是否为手动操作。

## 下一步

1. 实施阶段 2：五列不换行、状态视觉、无确认拖拽和键盘等价路径。
2. 实施阶段 3：关系驱动的聚焦问题引用块与双向 CAS。
3. 阶段 1–3 稳定后，以隔离合同开始 Helix→滴答单向投影。
