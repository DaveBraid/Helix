# 当前开发状态

最后更新：2026-08-13
当前基线提交：`96784c4 docs: record 1.0.1 publication`（分支 `dev`）
工作树状态：滴答分层门禁、回归测试与文档存在尚未提交改动。
当前阶段：滴答同步恢复计划第 1 阶段——基线审计与能力分层。

## 本轮目标

- 将滴答读取、普通写入、专用合同测试、项目投影拆成可独立验证和发布的安全门禁。
- 非目标：本轮不打开任何门禁，不访问真实滴答，不修改正式 Vault，不发布版本。

## 当前事实

- Project／Stage Markdown 保存身份、状态和正文；专用 Canvas 保存关系及布局；`data.json` 不保存项目真值。
- 新建项目必须同时填写项目名与首阶段名；项目和阶段可从关系图重命名，按 revision／CAS 原子写回 Markdown 标题与 Canvas 摘要；用户直接修改 Markdown 一级标题也会经稳定扫描反映到关系图。
- Stage `# 计划行动` 的顶层清单项自动进入任务页，缩进项作为子任务；首次读取会静默补稳定身份，空模板占位保持原样。
- 本地任务详情采用 Dense task canvas，支持日期月历、无时间／时间点／时间段及子任务增删改排；全部通过 Stage revision/CAS 写回原 Markdown，不建立第二份真值。
- Stage／Project 正文输入期间不运行派生项目扫描；失焦后合并刷新；后台 Helix 标签不抢编辑焦点。
- 项目启动等待 `onLayoutReady` 后读取双稳定快照；布局就绪前禁止项目写入。
- 切换项目聚焦只增量更新选中态和相机；结构变化离屏构建后原子替换，避免整页闪烁。
- 聚焦桥接以 Canvas 入边为关系源，支持推进、分支、合并、反向编辑及同窗竞争冲突。
- 聚焦引用在 Markdown 中只保留上下各一空行；Live Preview 折叠纯技术标记行，既有宽松空白会后台静默收敛。
- `helix-sequence` 保持物理文件高水位且不复用；`helix-stage-code` 按当前继承／分支／合并图维护，删除会原子更新现存 Stage 标题、属性、聚焦引用及 Canvas 编号索引。
- 冲突中心分级展示；文本使用真实行号 IDE diff，标量属性按需展开。
- `dev` 已把滴答能力拆成只读、普通任务写入、专用合同测试、项目投影四层；当前常量全部关闭，既有口令保留在 SecretStorage。
- 只读开放而普通写入关闭时，`sync()` 强制 pull-only；离线队列不被认领、重试或改变。服务层独立拒绝普通写入、合同写入与项目投影，界面和命令按对应能力隐藏。
- 版本 `1.0.1`，最低 Obsidian 桌面端 `1.12.2`。

## 本轮改动

- 修改 `release-capabilities`、服务、主入口、设置／任务视图与发布检查，建立四层能力门禁。
- 新增关闭态零远端调用、只读同步不消费队列及投影依赖关系回归；同步架构与开发闸门已更新。

## 相关约束

- 权威源、Stage 本地任务及项目投影边界见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 项目写入必须串行并使用 revision／CAS；不得覆盖未知正文或保存第二份本地任务真值。
- 本地子任务重排只允许同一父任务的直接子任务身份全集；身份缺失、重复或集合变化必须拒绝。
- 远端结果未知时禁止重试；同字段竞争必须逐字段人工选边。
- 真实滴答测试只可使用唯一合同标记和专用清单；不得删除、移动或覆盖用户已有远端对象。

## 当前验证

- 本轮完整门禁：69 个测试文件、980 项测试，以及 `typecheck`、`build`、`release:check`、`git diff --check` 全部通过。
- 既有实机验证：Stage 可持续编辑，后台 Helix 标签不抢焦；启动稳定复核会清除旧记录，首页与各主导航可渲染。
- 用户已实机确认项目聚焦和创建阶段不再闪烁；本地项目主流程可用。
- CLI 已重载 `1.0.1`：插件启用且版本正确，新建与重命名入口已挂载，错误缓冲为空。
- 发布安全扫描未发现凭证或本机绝对路径；本轮未访问滴答远端，测试 Vault `autoSync=false` 且口令保留。

## 未关闭问题

- 本地任务暂不支持跨 Stage／项目移动；需要多文件事务后再开放。
- 滴答 `items` 写入结果未知，项目滴答投影继续关闭。
- 2026-08-05 孤儿合同任务远端状态仍不确定，禁止盲目清理。

## 下一步

1. 运行完整自动化、构建和发布安全检查，修复回归。
2. 提交第 1 阶段并保持四层门禁全部关闭。
3. 仅开放只读门禁，在测试 Vault 通过 Obsidian CLI 验证真实清单、任务、习惯与专注读取；零远端写入。
4. 只读阶段稳定后，再准备专用临时清单写入合同。
