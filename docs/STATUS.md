# 当前开发状态

最后更新：2026-08-19
当前基线提交：`6f54572 fix: converge Dida task completion and detail editing`（分支 `dev`）
工作树状态：工作树干净；测试 Vault 的 Helix 项目已清空，滴答专用测试对象已清理。
当前阶段：普通滴答任务详情与完成／重开修复已验证，仍停在合并／发布之前。

## 本轮目标

- 让滴答任务复用主分支 Dense 详情壳层，并修复完成状态显示、动画和服务端完成时间收敛。
- 非目标：不发布、不触碰用户既有滴答对象或正式 Vault、不解除提醒／重复／检查项的只读门禁。

## 当前事实

- Project／Stage Markdown 是项目内容权威源，专用 Canvas 保存关系与布局；`data.json` 只保存恢复和同步状态。
- Stage `# 计划行动` 顶层条目进入任务页，缩进条目为本地子任务。
- 滴答能力按只读、普通写入、专用合同、项目投影分层；项目投影启用须 fresh 预览、版本化二次确认和运行态 readiness。
- 普通任务 CRUD、移动、完成／重开、冲突和父子关系已有当前 v13 合同证据。
- 本地与滴答任务详情共用 Dense task canvas 壳层；滴答独有属性集中在单一扩展区。
- Local 与 Remote 相同时零写入；中断写入重启后冻结，不得重发。
- 连接使用 unknown／online／offline 三态；离线编辑只排队，字段选择可保存但联网前不得应用。
- 限流状态持久化；冲突中心显示截止时间，到期后只允许一次受控读取，成功前不消费写队列。
- 项目映射为稳定 marker 父 Task；Stage 行动映射为带 `parentId` 和 marker 的子 Task，不使用 `items`。
- 项目删除精确排队删除远端子任务及父任务；开放任务集合证明不存在，tombstone 阻止重发和复活。
- 结果未知禁止重发；字段竞争保留三方快照，必须人工处理。
- 版本 `1.0.1`，最低 Obsidian `1.12.2`；当前不发布。

## 本轮改动

- 滴答任务详情改用与本地任务一致的 Dense 模态壳、标题层级和状态／优先级／日期／时间／标签矩阵；清单、提醒、重复与关联只留在一个默认折叠的滴答扩展区，原备注值未编辑时原样保留。
- 任务行按真实状态显示空圆圈或绿色勾选，完成／重开具有正确可访问语义；完成先播放局部反馈动画，再以验证后的状态重绘。
- 完成队列不再伪造本地完成时间；排队期间远端已完成且本地无其他业务改动时，采纳服务端 `completedTime` 并零写入收敛，不生成伪冲突或重开。
- 写入合同能力缺失时完成按钮明确禁用并解释原因，详情仍允许打开查看。

## 相关约束

- 权威源、能力缓存、合同清理、请求治理和项目投影规则见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 真实测试只创建唯一标记对象；删除前核对 ID、marker 和归属，结果未知时只读复核、绝不重发。
- 凭证只存 SecretStorage；正式 Vault 和用户既有滴答任务、清单、习惯、专注及配置均不在测试授权范围。

## 当前验证

- 本轮完整门禁：70 个测试文件、1001 项全部通过；`typecheck`、`build`、`release:check`、`git diff --check` 通过。
- 当前 v13 真实合同通过任务 CRUD、父子任务、项目投影、看板归栏、分栏创建和任务重开；账号时间语义为 `point`，全部专用对象已清理。
- Obsidian CLI 实机通过：远端详情显示 Dense 五属性矩阵且无重复时间／标签／优先级；唯一临时任务点击后立即出现完成反馈，远端复读仍为完成，随后按标题与专用正文双重身份删除。
- 结果未知领养、三方标题冲突、停用零写入、后台补齐和安全删除均完成过实测；专用对象最终清空，用户 5 个既有清单与收集箱未改，未触发限流。
- 当前测试 Vault 的项目、专用合同对象、队列、冲突和恢复项均为零；用户既有滴答对象未改。

## 未关闭问题

- 429／查询限流已有故障注入和持久治理；不得以真实限流验证。提醒、重复和检查项能力保持只读。
- 项目投影虽有合同证据，仍须完成普通任务体验稳定阶段后再按计划解除产品门禁。

## 下一步

1. 下一阶段补齐普通滴答任务编辑的针对性体验验收。
2. 普通任务稳定后再处理项目投影产品门禁；保持在 `dev`，不合并、不发布。
