# 当前开发状态

最后更新：2026-08-13
当前基线提交：`9c0ca51 fix: safely delete projected projects`（分支 `dev`）
工作树状态：仅本状态快照待提交。
当前阶段：项目同步已在 dev 开放；远端 v12、生产队列和项目删除安全事务已验证，持久配置保持默认关闭。

## 本轮目标

- 在不触碰用户既有滴答对象和正式 Vault 的前提下，完成项目单向同步的安全启用与首次生产验收。
- 非目标：本阶段不发布版本，不替用户选择生产清单／分栏，不自动启用远端写入。

## 当前事实

- Project／Stage Markdown 是项目内容权威源，专用 Canvas 保存关系及布局；`data.json` 只保存恢复与同步状态。
- Stage `# 计划行动` 顶层条目进入 Helix 任务页；缩进条目为本地子任务。
- 滴答能力分为只读、普通任务写入、专用合同和项目同步四层；dev 已开放代码门禁，用户配置仍默认关闭并要求版本化二次确认。
- 普通任务 CRUD、跨清单移动、完成／重开、逐字段冲突、重启队列和真实父子关系已有自动化与真实合同证据。
- 项目父级映射为带稳定 marker 的普通滴答 Task；Stage 行动映射为带稳定 `parentId` 和 marker 的普通子 Task，不使用父任务 `items`。
- v12 合同已覆盖正式项目同步编排器和生产 `OfflineQueue→queueDrain→写后收据` 创建／更新／完成／重开／删除闭环。
- 远端结果未知禁止重发；同字段竞争保留 Base／Local／Remote 并逐字段人工选择。
- 版本 `1.0.1`，最低 Obsidian `1.12.2`；本阶段不发布。

## 本轮改动

- v12 使用独立请求预算和持久清理计划，覆盖父子任务、状态、日期、分栏、生产队列完整生命周期与安全清理；失败诊断不泄露对象内容或身份。
- 项目投影远端结果未知会冻结且不重发；同字段竞争会阻塞并生成逐字段冲突；旧 items 队列、冲突和账本保持只读隔离。
- 设置页只读取选中的滴答清单，支持选择既有分栏或经预览和二次确认创建 `Helix项目` 分栏；启用也要求预览后二次确认，停用不删除远端对象。
- 项目投影激活新增版本凭证；历史无凭证 enabled 状态加载后自动降为禁用，保留映射与诊断但禁止升级即写。
- Stage Markdown 变更在项目刷新事务中自动纳管计划行动，再唤醒后台协调器；无需先打开任务页。
- 激活预览和最终确认会先在同一稳定工作区纳管计划行动；模板空复选框不算任务，不进入纳管或统计。
- 自动同步默认关闭；关闭时保存或替换 API 口令不会触发联网，手动同步与只读拉取仍可用。
- 设置页可二次确认清除滴答本地展示缓存；有未收口写入或项目同步启用时拒绝，且不清口令、能力缓存与事件账本。
- 删除已投影项目会先经既有队列精确删除行动与父任务；持久 tombstone 阻止未知结果重发及本地删除失败后的后台复活，只有本地入废纸篓成功才释放。

## 相关约束

- 权威源、能力缓存、合同清理、请求治理和项目同步复用规则见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 真实滴答测试只能创建本轮唯一标记对象；删除前必须精确核对 ID、名称、归属与集合，结果未知时只读复核、绝不重发。
- 凭证只存 SecretStorage；不得写入文档、日志、快照或测试输出。
- 正式 Vault 与用户既有滴答任务、清单和配置均不在测试授权范围内。

## 当前验证

- 最新完整门禁为 70 个测试文件、959 项全部通过。
- `typecheck`、`build`、`release:check`、`git diff --check` 已通过。
- v12 真实合同已完整通过：194 次主体调用，生产 OfflineQueue 创建／更新／完成／重开／删除及收据闭环成立；限流计数仍为零，临时对象、清理票据、队列、收据、冲突和恢复问题均为零。
- Obsidian CLI 实机：插件重载零错误；旧配置保持 `enabled=false`；设置入口正常；选中“科研”只读双源复读成功。临时本地行动自动纳管后已精确移除；只读激活预览统计 2 项目／8 行动／0 阻塞，队列、收据、恢复问题均为零，未执行远端写入。
- 测试 Vault 已清空滴答展示缓存；重载前后请求计数不变，`autoSync=false`、队列／冲突／恢复问题均为零，API 口令和 26 条历史事件保留。

## 未关闭问题

- 429／查询限流已有 API、持久治理器和离线队列分层故障注入；不得用真实限流验证。用户尚未选择生产目标，因此项目同步不会自动写入。
- 本地任务暂不支持跨 Stage／项目移动；提醒、重复和检查项能力保持只读。

## 下一步

1. 在现有合同专用远端环境内接入测试 Vault 的临时 Project／Stage 主链路探针，避免污染用户清单。
2. 重载后只读核对父子身份、通知、映射与零遗留队列。
3. 项目链路稳定后完成普通任务双向同步的最终完成审计。
