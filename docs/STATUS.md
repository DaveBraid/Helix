# 当前开发状态

最后更新：2026-08-23
父提交：`6afc36a fix: preserve Helix child task creation order`
工作树状态：本状态快照随测试 Vault 项目投影恢复修复提交更新；提交后应为干净工作树。
当前阶段：根因已确认并完成真实同步验证，继续停留在 `dev`，尚未进入发布流程。

## 本轮目标

- 查明测试 Vault 的进行中 Stage 为何没有进入远端 `Helix Projects`。
- 修复当前版本同目标停用配置无法恢复、单个冻结对象阻断其他 Stage 的问题。
- 安全收口合法行动 marker 后遗留可见文本的历史 Markdown。
- 非目标：不清理既有滴答对象，不切换远端目标，不发布版本。

## 根因

- 当前运行代码包含此前正式仓库修复，不是版本回退；`autoSync` 与项目投影配置按 Vault 独立保存，测试 Vault 的开关原本为关闭。
- 开启后，测试 Vault 保留当前 v3 激活、精确目标 ID 和历史映射，但配置为停用；自动引导把任何历史身份都当作旧版不兼容状态，无法原位恢复。
- 保留目标 ID `6a7ea40ce4b0a64fa9a6c3b2` 的远端展示名为 `📈Helix Projects`，当前代码只按新名称选择，进一步把同一目标误判为不存在。
- Stage-07 有冻结父任务检查点；激活层此前因任一冻结对象拒绝启用，扩大了冲突影响范围。
- Stage-09 有合法 v1 marker 后附可见 `⚪` 的历史行，严格解析把它视为损坏并阻断全库行动计数。

## 本轮改动

- 当前版本停用配置仅在目标稳定 ID、无分栏身份和确认凭证都精确一致时允许恢复；旧版、换目标、目标缺失继续拒绝。任一启用足迹失配时也不得回退到名称匹配或创建。
- 恢复身份门禁统一覆盖行动账本、父检查点、父 Base、待清理收据与分栏创建检查点。
- 同目标恢复优先采用持久化清单 ID，不依赖展示名，也不创建第二个目标。
- 同目标恢复保留冻结对象并开放其他 Stage；全新激活仍拒绝冻结身份。
- 启动迁移把合法 marker 后的可见后缀原样移到 marker 前；代码围栏、非清单行和真正损坏标记保持不动。
- `docs/ARCHITECTURE.md` 与 `docs/经验.md` 已同步恢复边界和 Vault 独立配置诊断经验。

## 真实验证

- 用户已在 `ObDevTestVault` 开启自动同步；插件重载后项目投影恢复为启用。
- Stage-06 已创建远端父任务 `6a8aecdce4b0f12600e5851e`，三个行动均回填远端 ID。
- 父子任务全部属于清单 `6a7ea40ce4b0a64fa9a6c3b2`；任务 1／2／3 的排序值依次递增，滴答展示顺序与 Helix 一致。
- Stage-07 与 Stage-09 的 conflict 检查点均保留，只冻结对应 Stage；Stage-06 已独立完成同步。
- Stage-09 的 `⚪` 可见文本完整保留，marker 已移到行尾。
- `npm test`：71 个测试文件、1043 项全部通过；定向 3 个测试文件、122 项通过。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过。
- Obsidian CLI 重载最终构建后，错误缓冲、error 与 warn 级控制台均为空。
- 独立项目主管首轮发现的两个 P1 门禁漏洞已修复；复审无剩余 P0/P1/P2。

## 未关闭问题

- Stage-07 与 Stage-09 仍有父任务 conflict，需要用户在冲突中心单独收口；不影响其他 Stage。

## 下一步

1. 形成测试 Vault 项目投影恢复的单一职责提交。
2. 后续修复继续在 `dev`；只有用户明确要求发布时才合入 `main`。
