# 当前开发状态

最后更新：2026-08-13
当前基线提交：`0ac92d4 feat: enable read-only Dida sync`（分支 `dev`）
工作树状态：专用合同门禁、可选能力隔离、回归与文档存在尚未提交改动。
当前阶段：滴答同步恢复计划第 3 阶段——专用写入合同已通过，准备固化。

## 本轮目标

- 将滴答读取、普通写入、专用合同测试、项目投影拆成可独立验证和发布的安全门禁。
- 非目标：本轮不打开任何门禁，不访问真实滴答，不修改正式 Vault，不发布版本。

## 当前事实

- Project／Stage Markdown 保存身份、状态和正文；专用 Canvas 保存关系及布局；`data.json` 不保存项目真值。
- 新建项目必须同时填写项目名与首阶段名；项目和阶段可从关系图重命名，按 revision／CAS 原子写回 Markdown 标题与 Canvas 摘要；用户直接修改 Markdown 一级标题也会经稳定扫描反映到关系图。
- Stage `# 计划行动` 的顶层清单项自动进入任务页，缩进项作为子任务；首次读取会静默补稳定身份，空模板占位保持原样。
- Stage／Project 正文输入期间不运行派生项目扫描；失焦后合并刷新；后台 Helix 标签不抢编辑焦点。
- 聚焦桥接以 Canvas 入边为关系源，支持推进、分支、合并和竞争冲突；Live Preview 隐藏技术标记。
- `helix-sequence` 保持物理文件高水位且不复用；`helix-stage-code` 按当前继承／分支／合并图维护，删除会原子更新现存 Stage 标题、属性、聚焦引用及 Canvas 编号索引。
- `dev` 已拆分四层滴答能力；当前开放只读与专用合同，普通任务写入和项目投影硬关闭，口令只在 SecretStorage。
- 只读开放而普通写入关闭时，`sync()` 强制 pull-only；离线队列不被认领、重试或改变。服务层独立拒绝普通写入、合同写入与项目投影，界面和命令按对应能力隐藏。
- 版本 `1.0.1`，最低 Obsidian 桌面端 `1.12.2`。

## 本轮改动

- `e485ffe` 已建立四层能力门禁，并补齐关闭态零远端调用、只读同步不消费队列及投影依赖关系回归。
- `0ac92d4` 仅开放读取层；任务／清单写入、专用合同与项目同步的命令和界面入口保持隐藏，服务门禁保持拒绝。
- 当前工作树独立开放专用合同；检查项未知结果经精确任务清理证明后只关闭该能力，不再抹掉普通 CRUD、看板和重开结果。

## 相关约束

- 权威源、Stage 本地任务及项目投影边界见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 项目写入必须串行并使用 revision／CAS；不得覆盖未知正文或保存第二份本地任务真值。
- 本地子任务重排只允许同一父任务的直接子任务身份全集；身份缺失、重复或集合变化必须拒绝。
- 远端结果未知时禁止重试；同字段竞争必须逐字段人工选边。
- 真实滴答测试只可使用唯一合同标记和专用清单；不得删除、移动或覆盖用户已有远端对象。

## 当前验证

- 本轮完整门禁：69 个测试文件、980 项测试，以及 `typecheck`、`build`、`release:check`、`git diff --check` 全部通过。
- Obsidian CLI 已在 `ObDevTestVault` 重载并完成真实只读拉取：6 个清单、73 项任务、1 条专注记录，队列／冲突／恢复问题均为 0，创建清单入口不存在，合同命令未注册；未执行远端写入。
- 习惯定义端点可读 1 项，但打卡端点返回滴答 `500 unknown_exception`；当前按可选能力失败保留旧缓存，界面显示“部分数据暂不可用”，不得宣称习惯打卡同步通过。
- 合同候选完整门禁：69 个测试文件、981 项测试及 `typecheck`、`build`、`release:check`、`git diff --check` 全部通过。
- 真实合同最终通过并安全清理：普通任务 CRUD、看板归栏、分栏创建、任务重开及 `point` 时间能力通过；提醒、重复规则、检查项保持只读。远端合同清单为 0，既有清单总数保持 5；本地清理票据、队列、冲突、恢复问题均为空，未触发限流。
- 既有实机验证：Stage 可持续编辑，项目聚焦和创建阶段不闪烁，本地项目主流程可用。
- 发布安全扫描未发现凭证或本机绝对路径；本轮未访问滴答远端，测试 Vault `autoSync=false` 且口令保留。

## 未关闭问题

- 本地任务暂不支持跨 Stage／项目移动；需要多文件事务后再开放。
- 滴答 `items` 在完成状态写入时结果未知且语义无法证明，项目滴答投影继续关闭。
- 2026-08-05 孤儿合同任务远端状态仍不确定，禁止盲目清理。

## 下一步

1. 提交并推送专用合同阶段。
2. 开放普通任务写入，先以新的专用临时清单验证创建、编辑、移动、完成、重开和删除；提醒、重复、检查项继续按能力隐藏或只读。
3. 验证离线队列、重启、逐字段冲突和限流恢复后，再开放后台双向同步。
