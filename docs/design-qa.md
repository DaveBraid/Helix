# 桌面界面视觉验收

最后更新：2026-07-30

## 对照范围

- Today 工作台：沿用用户选定的浅色第一稿；本周挑战使用深色霓虹活动卡。
- 项目工作台：参考 `andrewliang01/obsidian-research-card-manager@7fbd19388756419d89f3423d1ba774ddef767dbd` 的关系图、卡片、看板及画布导航。
- Helix 不复制上游的数据模型：项目与阶段正文仍在 Markdown，关系与位置仍在原生 Canvas。

## 已确认

- 左侧入口名称为“挑战”；顶部“正在进行”显示最多三项、所属项目和展开入口。
- 项目页的关系图、卡片和看板均可切换；项目和阶段卡片只是摘要入口，点击后打开 Markdown。
- 关系图支持缩放、适应全部、空格平移、多选和拖动；拖动时连线实时更新。
- Canvas 节点坐标会忠实投影到内嵌关系图；为避免原生节点尺寸把摘要卡片撑出大面积空白，Helix 使用统一的 `248×128` 显示尺寸，并保留 Canvas 原宽高不作静默改写。项目状态在项目条和项目卡片中可见。
- 顶部只保留“新建项目”。阶段卡片右侧中部在悬停或选中时显示加号；单卡点击已经确定来源，并按当前后继直接推断继承或分支，不再弹出关系和前置阶段选单。
- 新阶段弹窗只显示自动编号、推断后的关系摘要和阶段标题输入；第一次点击创建继承，第二次从同一来源点击时把既有继承转换成分支并创建一个新阶段。
- 关系图中多选两个及以上阶段后点击其中一个加号，直接进入合并标题输入；合并摘要按当前拖动位置排列来源，并预告哪些来源的既有继承会改为分支。跨项目合并明确显示目标项目名、外部来源及安全确认。
- 已有连线可选择，画布底部显示关系摘要和“修改或删除”；关系弹窗可以调整前置阶段或二次确认删除，关系类型只展示由完整拓扑自动判定的规则。
- 阶段卡片右上角在悬停或选中时显示删除入口；删除弹窗清楚说明 Markdown 与 Canvas 影响并要求勾选确认。
- 实机已从阶段 4 创建并删除阶段 9，验证了中间节点分支、右侧/下移排布、确认弹窗和 Canvas 关系清理。
- 最新精确构建已从阶段 7、阶段 8 的加号分别验证分支和继承摘要：弹窗自动聚焦标题输入，只保留取消与创建按钮，不再出现关系或前置阶段选单。
- 分析页热力图采用年度整条布局：12 个月横向铺满，每个月以独立圆角轮廓包住七列、六行的日期方格；窄窗口横向滚动，不把月份拆成多行。全年共享同一强度色阶，非本月补位格透明且不进入无障碍日期标签；“任务完成／习惯打卡”切换会同步更新统计单位和每日标签。
- 提交 `9bb6c4c` 后已重新回归 Today 展开/收起、任务编辑字段、复盘入口、周/月挑战详情、分析页、冲突空状态以及项目关系图/卡片/阶段看板。测试库两个重叠项目节点已在原生 Canvas 中分开。
- 多选 11 个阶段后的真实合并弹窗已验证自动编号、合并来源、既有继承转分支预告与跨项目确认；操作在提交前取消，未生成新阶段。
- 人工三方冲突已逐字段选择本地、远端和自定义值；只有四个竞争字段全部明确后才允许应用。未执行最终远端复检或写入，测试夹具已删除，重载后的冲突中心为空。
- 紧凑显示几何的上一阶段补充测试已通过项目主管二次冻结审查。
- 已在同一 `768px` Obsidian 窗口中运行上游关系画布并与 Helix 对照：当前卡片密度和缩放控件无需继续收缩；项目切换条是跨项目关系所需上下文。阶段看板在窄窗口中保持固定列宽并横向浏览，无文字截断。上游临时插件和夹具已清理。
- 阶段删除持久事务日志已覆盖“Canvas 已提交后继续删除”“Markdown 被修改后回滚”“Canvas 出现未知版本后冻结”“原路径被无关 Markdown 占用”“日志清理失败”“Canvas 前后两次日志复检”“冻结取消在途写入”和“data.json 只读冻结”路径；尚需一次真实重载恢复回归。
- 本轮项目图谱重构已通过项目主管最终冻结审查，P0/P1/P2 均为 0。
- 第三阶段项目页重构已在真实 Obsidian 1.12.7 中验证：项目色渐变在深浅主题均保持低调和文字可读；closed 阶段显示绿色勾选与“已完成”；连接把手具备键盘可达目标选择；“全部项目”在卡片视图真实显示两个项目，选择单项目时真实过滤；无可折叠连通分量时不显示空操作按钮。
- 关系图使用内存 origin 和四向动态扩边；显式“整理全部”、项目独立色彩入口、全部项目入口均已在 768px 窗口显示。连接确认、删除桥接和折叠投影的最终实机写入保留为下一阶段人工鼠标验收。
- 最新完整重载后已真实执行“整理全部”和“适应全部”：大于四个视口的布局不再受 25% 最小缩放限制，12 个阶段可同时进入 768px 窗口。键盘连接目标不含来源本身，最终确认明确显示“来源项目/阶段 → 目标项目/阶段”；操作在最终写入前取消。
- 自动化已验证折叠头不可拖动写回、全选和键盘目标不含折叠分量成员、跨项目聚合边展开所有折叠端、pointercancel/失去捕获/空白释放零提交、四向扩边保持 `raw = screen - origin` 且在 `0.0004` 缩放下仍一次补足 1800px 屏幕余量、新项目只布局自身且避让既有卡片。

## 未关闭问题

- 框选仍是后续增强。直接拖线已有完整 pointer 路径、目标高亮、预览虚线和键盘替代入口；Computer Use 未稳定命中拖拽源点，因此最终提交前仍需一次人工鼠标拖线确认。
- 最新构建的开发者控制台已复查且没有 Helix 错误。

代码与自动化冻结已通过；在上述人工鼠标项关闭前，本文件不作“完整桌面交互最终通过”结论。
