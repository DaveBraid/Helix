# 桌面界面视觉验收

最后更新：2026-07-31

## 对照范围

- Today 工作台：沿用用户选定的浅色第一稿；本周挑战使用深色霓虹活动卡。
- 项目工作台：参考 `andrewliang01/obsidian-research-card-manager@7fbd19388756419d89f3423d1ba774ddef767dbd` 的关系图、卡片、看板及画布导航。
- Helix 不复制上游的数据模型：项目与阶段正文仍在 Markdown，关系与位置仍在原生 Canvas。

## 已确认

- 左侧入口名称为“挑战”；顶部“正在进行”显示最多三项、所属项目和展开入口。
- 项目页的关系图、卡片和看板均可切换；项目和阶段卡片只是摘要入口，点击后打开 Markdown。
- 关系图支持缩放、适应全部、空格平移、多选和拖动；拖动时连线实时更新。
- Canvas 节点坐标会忠实投影到内嵌关系图；为避免原生节点尺寸把摘要卡片撑出大面积空白，Helix 使用统一的 `248×128` 显示尺寸，并保留 Canvas 原宽高不作静默改写。项目状态在项目条和项目卡片中可见。
- `dev` 新增关系图项目排序菜单：列表行可拖动，提交后按显式顺序重新排列项目容器；Shift 吸附同时支持阶段卡片和整个项目容器，并以虚线双向箭头显示当前 X／Y 锚点。Obsidian CLI 已确认菜单布局、拖动属性、阶段吸附引导出现及释放后清理；容器吸附由相同纯函数、结构接线和服务合同自动化覆盖，尚未用多项目实机拖动冒充验证。
- 顶部只保留“新建项目”。阶段卡片右侧中部在悬停或选中时显示加号；单卡点击已经确定来源，并按当前后继直接推断继承或分支，不再弹出关系和前置阶段选单。
- 新阶段弹窗只显示自动编号、推断后的关系摘要和阶段标题输入；第一次点击创建继承，第二次从同一来源点击时把既有继承转换成分支并创建一个新阶段。
- 关系图中多选两个及以上阶段后点击其中一个加号，直接进入合并标题输入；合并摘要按当前拖动位置排列来源，并预告哪些来源的既有继承会改为分支。跨项目合并明确显示目标项目名、外部来源及安全确认。
- 已有连线可选择，画布底部显示关系摘要和“修改或删除”；关系弹窗可以调整前置阶段或二次确认删除，关系类型只展示由完整拓扑自动判定的规则。
- 阶段卡片右上角在悬停或选中时显示删除入口；删除弹窗清楚说明 Markdown 与 Canvas 影响并要求勾选确认。
- 实机已从阶段 4 创建并删除阶段 9，验证了中间节点分支、右侧/下移排布、确认弹窗和 Canvas 关系清理。
- 最新精确构建已从阶段 7、阶段 8 的加号分别验证分支和继承摘要：弹窗自动聚焦标题输入，只保留取消与创建按钮，不再出现关系或前置阶段选单。
- 分析页热力图采用年度整条布局：12 个月横向铺满，每个月以独立圆角轮廓包住七列、六行的日期方格；窄窗口横向滚动，不把月份拆成多行。全年共享同一强度色阶，非本月补位格透明且不进入无障碍日期标签；“任务完成／习惯打卡”切换会同步更新统计单位和每日标签。
- 提交 `9bb6c4c` 后已重新回归 Today 展开/收起、任务编辑字段、复盘入口、周/月挑战详情、分析页、冲突空状态以及项目关系图/卡片/阶段看板。测试库两个重叠项目节点已在原生 Canvas 中分开。
- 多选 11 个阶段后的真实合并弹窗已验证自动编号、合并来源、既有继承转分支预告与跨项目确认；操作在提交前取消，未生成新阶段。
- 人工三方冲突已逐字段选择本地、远端和自定义值；只有四个竞争字段全部明确后才允许应用。未执行最终远端复检或写入，测试夹具已删除，重载后的冲突中心为空。
- 紧凑显示几何的上一阶段补充测试已通过项目主管二次冻结审查。
- 已在同一 `768px` Obsidian 窗口中运行上游关系画布并与 Helix 对照：当前卡片密度和缩放控件无需继续收缩；项目切换条是跨项目关系所需上下文。阶段看板在窄窗口中保持固定列宽并横向浏览，无文字截断。上游临时插件和夹具已清理。
- 阶段删除持久事务日志已覆盖“Canvas 已提交后继续删除”“Markdown 被修改后回滚”“Canvas 出现未知版本后冻结”“原路径被无关 Markdown 占用”“日志清理失败”“Canvas 前后两次日志复检”“冻结取消在途写入”和“data.json 只读冻结”路径。真实删除“Helix E2E 回归 / 链路中段”后，前后阶段已桥接，跨项目后继仍保留；随后预置未完成隐藏日志并强制重载，界面提示恢复 `completed`，日志与已删除阶段均无残留。
- 第三阶段项目页重构已在真实 Obsidian 1.12.7 中验证：项目色渐变在深浅主题均保持低调和文字可读；closed 阶段显示绿色勾选与“已完成”；连接把手具备键盘可达目标选择；“全部项目”在卡片视图真实显示两个项目，选择单项目时真实过滤；无可折叠连通分量时不显示空操作按钮。
- 关系图使用内存 origin 和四向动态扩边；显式“整理全部”、项目独立色彩入口、全部项目入口均已在 768px 窗口显示。删除桥接与跨项目确认已经真实写入；真实鼠标拖线可显示虚线预览，但 Computer Use 无法稳定送达释放事件，最终鼠标释放保留为用户补验。
- 最新完整重载后已真实执行“整理全部”和“适应全部”：大于四个视口的布局不再受 25% 最小缩放限制，12 个阶段可同时进入 768px 窗口。键盘连接目标不含来源本身，最终确认明确显示“来源项目/阶段 → 目标项目/阶段”；操作在最终写入前取消。
- 自动化已验证折叠头不可拖动写回、全选和键盘目标不含折叠分量成员、跨项目聚合边展开所有折叠端、pointercancel/失去捕获/空白释放零提交、四向扩边保持 `raw = screen - origin` 且在 `0.0004` 缩放下仍一次补足 1800px 屏幕余量、新项目只布局自身且避让既有卡片。
- 项目切换条已按用户参考图与后续修改完成对照：所有项目均为独立胶囊，调色盘进入胶囊，选中和未选中状态都有轮廓与轻底色。持久对照图见 `docs/assets/project-strip-comparison.png`，上半为用户参考，下半为实现。
- 项目容器使用项目色的单一透明填充而非渐变，网格可透出；项目切换会先适应该项目容器再居中。阶段 3 → 阶段 11 的第二条入链已真实写入并正规化为合并，整理后来源阶段以受控动画回到视口中心；深色证据见 `docs/assets/structure-focus-dark.jpeg`。
- 浅色主题下胶囊、容器、关系边、完成标记和半透明操作按钮均清晰，证据见 `docs/assets/project-light.jpeg`。
- 最新精确构建已真实验证连续两个 closed 阶段折叠与展开：折叠后只保留阶段 1 头节点及“已折叠 2 个完成阶段”摘要，容器入口切换为“展开已完成”；证据见 `docs/assets/completed-fold-dark.jpeg`。折叠前后 `{nodes, edges}` 的规范 SHA-256 均为 `1def81e37c8fb54ca41b0b95ea9f71be34c5b2f171cf4979bfe8066d065dc4d2`，仅 `helixCompletedCollapse.projectIds` 变化；展开后恢复空集合。开发者控制台只有 Obsidian 欢迎消息，没有 Helix 错误。
- 前一阶段的 `127%` 项目聚焦和 `250%` 节点聚焦截图保留为历史对照，不再代表当前缩放策略。
- 当前精确构建把所有显式聚焦的最高缩放统一收紧为 `120%`：点击 Test1 后完整容器以 120% 居中；双击阶段 1 后相机保持 120% 并把节点中心移到画布中心。两条路径均保留 420ms 动画，节点内容与操作按钮未裁切。
- 深色主题已逐页回归今日、任务、项目、复盘、分析、挑战和冲突。正文、次要信息、胶囊、状态色、输入控件和空状态均使用主题语义色，未再发现黑字压在深色表面上的主要可读性缺陷。
- 卡片右侧为标题预留额外空间；删除、加子阶段和连接三个按钮统一为半透明圆形表面，SVG 在两个轴向均居中，按钮与标题不再贴靠。
- 点击“全部项目”后，相机以全部容器的内容边界恢复到约 39%，三个项目在同一视口完整居中，证据见 `docs/assets/all-projects-focus-dark.jpeg`。相机插值的起点、中点和终点另有纯函数回归测试；减少动态效果偏好会直接跳到终点。
- 项目容器和阶段卡片的状态胶囊现为可操作按钮。真实将项目由进行中切换为暂停、阶段由进行中切换为已完成后，界面与读屏树都更新当前状态；恢复原状态后测试 Markdown 无额外正文变化。
- 最新精确构建已用真实鼠标轨迹框选“Helix E2E 回归”的两个阶段：两张卡片同时出现选择描边，加号读屏语义改为“合并所选 2 个阶段”，弹窗列出“项目启动 + 链路末端”。取消弹窗后没有产生结构写入。反向拖动、边界相交和 2 万节点线性扫描另有自动化回归。

## 未关闭问题

- 直接拖线已有完整 pointer 路径、目标高亮、预览虚线和键盘替代入口；Computer Use 未稳定命中拖拽源点后的最终释放，后续仍需一次人工鼠标拖线确认，不是当前阶段提交闸门。
- 最新构建的开发者控制台已复查且没有 Helix 错误。

本轮项目/阶段状态编辑、框选、项目胶囊、透明容器、结构回焦、项目/全局标签聚焦、卡片双击聚焦、深浅主题和完成折叠视觉验收通过；真实鼠标拖线释放继续作为后续人工补验，不阻断当前阶段。
