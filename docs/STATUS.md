# 当前开发状态

最后更新：2026-08-22
当前基线提交：`7d94b8b release: prepare Helix 2.1.1`
工作树状态：工作树干净。
当前阶段：`2.1.1` 发布候选已在 `dev` 完成门禁，等待合入 `main`。

## 本轮目标

- 发布包含增量节点布局、未保存布局保护和祖先链路高亮的 Helix `2.1.1`。
- 非目标：不新增滴答能力、不启用或访问滴答远端、不修改正式 Vault。

## 当前事实

- Project／Stage Markdown 是项目身份、状态、正文与本地任务的权威源；专用 Canvas 是关系和布局的权威源。
- 日常开发只在 `dev`；只有用户明确要求“发布新版”时才允许合入 `main`、打标签和发布 Release。
- `2.1.0` 仍是当前远端正式版；`2.1.1` 发布候选已在 `dev` 准备完成。
- Stage `# 计划行动`直接进入任务页并形成任意深度本地任务树；带子任务的父任务完成状态由直属子任务派生。
- 项目关系图支持推进、分支、合并、关系中插、项目显式排序、容器／阶段 Shift 吸附、作用域整理及布局保存读取。
- Canvas `helixProjectOrder` 维护自动布局的项目容器顺序；旧库无账本时按当前容器位置推导，新项目追加到末尾。
- 新建项目或阶段只设置新增节点 X/Y；所有既有托管节点的 X/Y 逐项保持不变，容器按节点外框自动扩展。
- 结构写入后，未竞争的既有节点继续使用创建前的会话坐标；新节点相对显示父节点平移，外部已移动的 Canvas 节点不被旧草稿覆盖。
- 连续“推进”位于父节点右侧并继承其 Y；新分支从既有后继下方展开，合并位于最右父节点下一列且使用父节点平均 Y。
- 悬停阶段卡片时，当前卡片、所有反向可达父阶段及路径边保持原样；后继和无关卡片／边弱化，合并会保留全部上游分支。
- 悬停高亮仅是会话投影，不写 Markdown、Canvas、设置或撤销栈。
- 自动同步仍需显式启用；当前测试 Vault 的自动同步为关闭状态。

## 本轮改动

- `manifest.json`、`package.json`、`package-lock.json` 与 `versions.json` 已统一为 `2.1.1`，最低 Obsidian 仍为 `1.12.2`。
- `README.md` 与 `docs/releases/2.1.1.md` 已说明升级范围、数据边界、已知限制和许可证。

## 相关约束

- 权威边界、关系 DAG、布局与会话投影合同见 [ARCHITECTURE.md](ARCHITECTURE.md) 第 5 节。
- 创建提交不得修改任何既有节点的 X/Y；视图重建也不得丢弃未保存会话坐标，容器扩展仅是 UI 投影。
- 悬停高亮必须遍历全部托管入边；不得把后继、无关节点或项目兼容锚点误算为上游链路。
- Canvas 写入继续使用 revision CAS；未知字段、非托管节点及用户 Markdown 正文不得静默改动。
- 凭证只存 SecretStorage；冲突禁止静默覆盖；远端结果未知禁止重发。

## 当前验证

- 完整门禁通过：71 个测试文件、1022 项；`typecheck`、`build`、`release:check`、`git diff --check` 全部通过。
- 自动化覆盖新增节点时合并未保存布局、连续增长保持分支行，以及外部 Canvas 移动优先于旧草稿。
- 自动化覆盖菱形合并 DAG：悬停合并节点保留两个来源分支及共同根，排除后继和无关链路。
- Obsidian CLI 按真实时序验收：Canvas 基线中的“并行分支”为 `(816,1172)`，未保存会话位置 A 为 `(559.33,1297.83)`；连续新增分支和其推进后，A 不变，新推进节点为 `(1375.33,1697.83)` 并继承父节点 Y；Canvas 旧节点仍未改写且会话保持待保存。
- CLI DOM 验收真实关系图：悬停目标时仅目标、两级父节点与两条上游边保持高亮，离开后所有渐隐类清零。
- 插件错误缓冲和 error 控制台均为空；自动同步为 `false`，未访问滴答或正式 Vault。
- Obsidian CLI 已重载 `2.1.1` 构建：插件启用、非恢复模式、自动同步关闭，错误缓冲为空。
- 本轮用 Obsidian CLI 同时读取工作台会话布局与 Canvas 权威坐标，覆盖后台刷新稳定后的最终状态；未使用截图比对。

## 未关闭问题

- 用户已确认本轮位置保持修复成功。
- 普通滴答子任务新增、删除和排序仍受真实合同能力约束，不属于本轮范围。

## 下一步

1. 提交 `2.1.1` 发布候选并推送 `dev`。
2. 将 `dev` 快进合入 `main`，创建并推送 `2.1.1` 标签。
3. 上传完整 BRAT 附件并核对 GitHub Release；发布后同步 `dev` 与 `main`。
