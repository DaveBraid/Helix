# 当前开发状态

最后更新：2026-08-21
当前基线提交：`db7dba4 fix: persist project reorder and align equal gaps`
工作树状态：工作树干净。
当前阶段：项目排序持久化修复与同层级等间距吸附已在 `dev` 完成；暂不发布。

## 本轮目标

- 修复项目排序拖动后未落盘；移除排序按钮图标；让 Shift 对齐在阶段卡片和项目容器各自层级内支持同轴与等间距吸附，并使用纯虚线提示。目标已完成。
- 非目标：不合并 `main`、不发布版本、不启用或测试滴答远端、不修改正式 Vault 数据。

## 当前事实

- Project／Stage Markdown 是项目身份、状态、正文与本地任务的权威源；专用 Canvas 是关系和布局的权威源。
- `2.0.0` 已正式发布；`main` 与 `dev` 在本轮开始时同步于 `e6bde81`。
- Stage `# 计划行动`直接进入任务页并形成任意深度本地任务树；编辑使用 Stage revision CAS，不在 `data.json` 保存影子任务。
- 有子任务的本地行动与 Stage 主任务完成状态为派生值：全部直属子任务完成后自动完成，任一子任务重新打开后自动恢复未完成。
- 项目关系图支持推进、分支、合并、关系中插、容器级整理、布局保存读取、项目／阶段改名和颜色编辑。
- 关系图完整自动布局现在以调用方项目顺序作为容器 lane 顺序；Canvas `helixProjectOrder` 是该显式顺序的内部账本。
- 无排序账本的旧 Canvas 按当前阶段容器的上、左位置推导兼容顺序；账本缺少的新项目追加，已删除的旧 ID 忽略。
- 关系图 Shift 吸附独立处理 X、Y 轴与等间距候选；阶段只使用其他可见结构阶段，项目容器只使用其他项目容器，禁止跨层级吸附。
- 自动同步仍需显式启用；当前测试 Vault 的自动同步为关闭状态。

## 本轮改动

- `src/services/project-workspace.ts`：读取／校验项目顺序账本，新增完整排列与 Canvas revision 校验的 `reorderProjects`，一次 CAS 保存顺序并重排全图。
- `src/domain/project-graph.ts`：完整自动布局严格采用传入项目顺序，不再由旧坐标二次排序。
- `src/ui/project-lineage-workbench.ts`、`src/ui/helix-view.ts`：工具栏最右提供无图标项目排序菜单；`drop` 与 `dragend` 共用一次性提交，避免未落在接收区时排序丢失；Shift 吸附返回同轴或等间距位置和纯虚线段，覆盖卡片和项目容器。
- `styles.css`：排序弹层、拖拽行、容器吸附轮廓与纯虚线 SVG 引导样式。
- 相关单元、服务合同与结构测试已增加；架构和设计 QA 文档同步更新。

## 相关约束

- 权威边界、Canvas 排序账本、布局与拖动合同见 [ARCHITECTURE.md](ARCHITECTURE.md) 第 5 节。
- 项目排序必须是当前全部项目的无重复排列；写入绑定打开时 Canvas revision，竞争时零写入拒绝。
- 排序只写内部账本和托管节点坐标，未知 Canvas 字段、非托管节点、Markdown 正文不得改动。
- 对齐引导是纯会话投影；只有用户明确保存当前布局时才写 Canvas。
- 凭证只存 SecretStorage；冲突禁止静默覆盖；远端结果未知禁止重发。

## 当前验证

- 完整门禁通过：71 个测试文件、1015 项；`typecheck`、`build`、`release:check`、`git diff --check` 全部通过。
- 服务合同覆盖项目排序的完整排列校验、Canvas revision 竞争拒绝、账本持久化和重排；领域测试覆盖显式 lane 顺序与独立 X／Y 锚点。
- Obsidian CLI 已用两个项目完成排序反转、Canvas `helixProjectOrder` 核对、重新打开菜单核对；随后仅触发 `dragend`、不触发 `drop` 也成功恢复原顺序与账本，证明本轮丢失修复有效。
- CLI 确认“项目排序”按钮文本正确且不含 SVG 图标。
- 真实 CDP 指针已验证项目容器 Shift 拖动：吸附时出现 1 条纯虚线、无 marker 箭头且容器进入吸附态；释放后引导线为 0，会话位移已撤销，错误缓冲为空。
- 等间距领域测试覆盖阶段／容器共用的水平与垂直间距计算；结构测试锁定两个层级使用互相隔离的候选集合。
- 自动同步确认仍为 `false`；本轮未访问滴答，未修改正式 Vault。

## 未关闭问题

- 多项目真实使用时仍可继续观察等间距吸附阈值是否需要按手感微调；当前合同与实机路径均已通过，不构成阻塞。
- 普通滴答子任务新增、删除和排序仍受真实合同能力约束，不属于本轮范围。

## 下一步

1. 由用户在含多个项目的关系图中体验排序及阶段／容器等间距吸附，记录阈值或视觉问题。
2. 保持改动留在 `dev`；收到确认后再决定是否合入 `main` 与发布。
3. 后续滴答开发继续遵守专用临时对象与显式远端写门边界。
