# 当前开发状态

最后更新：2026-08-04
当前基线提交：`eba9f8a fix: refine today dashboard metrics`
工作树状态：工作树干净；用户正修改的 `Template/Helix/` 与运行态 `data.json` 均不纳入 Git。
当前阶段：首页今日投影与专注统计纠正已提交；下一阶段继续项目 Canvas 和复盘交互打磨。

## 本轮目标

- 修正首页时间感知、今日任务投影与专注统计，保持滴答只读同步边界。
- 非目标：不扩展任务管理；冲突实测、项目—滴答任务联动延期；不接远端、不发布、不操作用户既有滴答或 Obsidian 数据。

## 当前事实

- 滴答清单是任务、远端清单、习惯和专注记录的权威源；Markdown/Canvas 是 Helix 项目、阶段正文、关系和布局的权威源，见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 滴答合同版本为 4，旧版本或不同授权缓存均失效为只读；提醒、重复和父子保持只读。两轮专用真实合同对象已精确清理并只读证明零残留，未触及用户数据。
- 写入仅在当前授权的有效合同下单发；限流只读复核，身份不明或结果未知即冻结，不能重发。同步仍可拉取且不排空待写队列。
- 看板派生 `columnName`、滴答 App 管理的 `status`／`completedTime` 不进入可写三方冲突；CLI/设置确认隔离、15 秒失效、运行单飞，卸载静默。
- 工作台导航顺序为今日、项目、任务、复盘、挑战、冲突；复盘页承载数据回顾，独立分析页已移除。主内容区独立滚动，侧栏和页头不随其滚动。
- 模板目录为 Vault 相对路径，默认 `Template`，六份默认模板位于 `Template/Helix/`；创建只消费模板正文，保留 frontmatter、H1 与自动摘要边界。首次安装待确认；迁移仅补缺失文件且不覆盖自定义模板。
- 首页问候按本地日期和时段稳定选择；今日任务复用日视图的日期／任务时区投影，仅显示仍待推进的今天任务。
- 专注 `duration` 以毫秒换算，并在可靠起止时间存在时交叉校验；同步可替换同一确定性 ID 的本地派生专注分钟值，不改远端或其他事件。

## 本轮改动

- 首页：稳定时段问候、今日任务空态／计数和任务时区投影。
- 同步：专注毫秒时长、旧派生事件纠正与挑战领取状态重算；测试和架构文档同步更新。

## 相关约束

- 凭证仅存 Obsidian SecretStorage；不得进入仓库、`data.json`、日志、Markdown、Notice 或测试夹具。
- 真实写入只限唯一标记的专用对象；身份不明、远端结果未知、`manualCleanupRequired` 或残留未知时立即停止，禁止盲目重发。
- 项目、同步与 UI 分层，以及 Markdown/Canvas 写入边界见 [ARCHITECTURE.md](ARCHITECTURE.md)；任何冲突禁止静默覆盖。
- 不得把本地测试调用量视为真实账号配额，或据此加入猜测性限速。

## 当前验证

- 已通过：全量门禁为 54 个测试文件、555 项测试，以及 `typecheck`、`build`、`release:check`、`git diff --check`。
- 已通过：模板路径／渲染／并发／回滚、恢复模式零写入、项目初始及分支阶段批量模板、四类复盘模板、导航模型行为与布局静态守卫；合同失效、跨重启只读、队列零排空、字段白名单、冲突冻结、看板门禁及限流仍有专项覆盖。
- 实机：Obsidian 1.13.4 CLI 重载后 `dev:errors` 为 0；首页展示当前时段问候和真实今日计数，复盘页专注统计已由错误的 47,117 分钟纠正为 47 分钟。
- 实机：纠正前待写队列为 0；同步后队列、冲突和恢复问题仍均为 0，只刷新本地派生专注分钟，没有改动远端记录。
- 实机：六份默认模板已生成于 `Template/Helix/`，重载前后逐文件 SHA-256 不变；设置页真实显示 `Template` 路径、补齐按钮与不覆盖说明。
- 审查：看门狗独立复核与门禁全绿，P0/P1/P2 均为 0，已放行提交。

## 未关闭问题

- 真正首次安装的模板目录弹窗仅有自动化覆盖；为保护当前 `data.json`，未在现有安装上伪造首次安装状态。不得把延期的任务写入能力混入本阶段。
- 真实 Base/Local/Remote 手动冲突合同与项目—滴答稳定 ID 联动已延期，当前不属于任务管理收口范围。

## 下一步

1. 复核项目 Canvas 容器、节点关系、折叠与自动布局，形成可执行的交互验收清单。
2. 打磨复盘的项目／任务引用、周期导航与模板问答体验，不恢复延期的滴答写入扩展。
3. 发布前在隔离新 Vault 补做真实首次安装模板弹窗验收。
