# 当前开发状态

最后更新：2026-08-20
当前基线提交：`5fd1bac docs: record 1.0.2 publication`（临时分支 `merge/main-dev-20260820` 正在合并本地 `dev`）
工作树状态：合并改动已完成语义融合和验证，等待阶段提交。
当前阶段：以 `main` 的 1.0.2 项目画布为基线，并入 `dev` 的滴答同步、统一任务详情、递归任务树和性能修复。

## 本轮目标

- 完整保留 1.0.2 项目画布、阶段编号和关系中插行为，同时合并开发分支新增能力。
- 非目标：本轮不发布版本、不访问真实滴答、不修改正式 Vault 数据、不自动开启用户已有配置中的远端写入。

## 当前事实

- Project／Stage Markdown 是项目身份、状态和正文的权威源；专用 Canvas 是关系和布局的权威源。
- main 的 Shift 同轴吸附、容器级整理、保存布局读取、关系中插、分支编号槽位、项目颜色编辑和单箭头悬停加号均已保留。
- Stage `# 计划行动`直接进入任务页，缩进项形成任意深度本地任务树；写回使用 Stage revision CAS。
- 普通滴答任务、Stage 行动和项目投影父任务共用统一任务详情页，不建立第二份任务真值。
- 滴答读取、普通任务写入、合同测试和项目投影使用独立发布能力门禁；自动同步和项目投影仍要求显式配置及运行时 readiness。
- 项目投影复用 `HelixService`、唯一离线队列、共享远端写门和冲突合并管线，不另建远端写入引擎。
- 旧 Task Reference Markdown 保持原文件不动，但运行时服务和测试已由 dev 删除。
- 当前版本文件仍为 `1.0.2`，最低 Obsidian `1.12.2`；本轮不生成 Release。

## 本轮改动

- 从 `main` 新建 `merge/main-dev-20260820`，以非快进方式合入本地 `dev` 的 118 个独有提交。
- 自动融合 `src/main.ts`、`project-workspace.ts`、`helix-view.ts`、关系图工作台和样式；逐项确认 main 与 dev 的关键接口同时存在。
- 接受 dev 对旧 `task-references` 运行时的删除，并加入统一任务详情、任务树、v13 项目投影合同及同步恢复能力。
- `STATUS.md` 不采用任一分支旧流水账，按合并后的当前事实重写。

## 相关约束

- 权威边界、远端写门与任务详情适配见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 凭证只存 SecretStorage；冲突禁止静默覆盖；远端结果未知禁止重发。
- 项目与同步写入必须使用稳定身份、revision／CAS 和精确远端 ID；不得按标题猜测或触碰用户已有远端对象。
- 合并验证期间保持测试 Vault 自动同步关闭；未经用户明确安排不得运行真实滴答合同。
- 相同领域逻辑必须复用现有服务，禁止为项目任务、普通任务或多视图复制第二套实现。

## 当前验证

- 合并专项回归通过：10 个测试文件、342 项测试，覆盖项目图谱、阶段编号、项目工作区、统一任务详情、递归任务树、刷新与项目同步状态机。
- 完整门禁通过：71 个测试文件、1007 项测试；`typecheck`、`build`、`release:check`、`git diff --check` 全部通过。
- Obsidian CLI 实机通过：插件以 1.0.2 重载，根视图和六个导航入口正常；项目页渲染 8 张阶段卡、7 条关系命中层和中插入口；任务页渲染 82 行任务与 6 个递归折叠入口；错误缓冲为空。
- CLI 验收未使用 Computer Use。旧实例卸载曾回写内存中的开启状态；停用插件后已离线把测试 Vault `autoSync` 与项目投影同时关闭，再启用复核均为 false。
- 本轮未运行真实滴答合同，未主动访问或写入远端，未修改正式 Vault。

## 未关闭问题

- 合并提交完成后需把本文件基线更新为新的合并提交。
- 普通滴答子任务新增、删除和排序仍受真实合同能力约束；不得用项目投影证据替代。

## 下一步

1. 形成临时分支合并提交，并把本快照更新到新的合并基线。
2. 用户确认临时分支验收结果后，再决定是否合入 `main`；本轮不发布。
3. 后续真实滴答测试继续使用专用临时对象和明确授权，保持测试 Vault 开关默认关闭。
