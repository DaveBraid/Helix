# AGENTS.md

本文件是 Helix 项目的稳定入口。任何智能体或开发者开始工作前，必须先阅读本文件，再按任务需要阅读 `docs/` 下对应文档。

## 项目目标

Helix 是桌面优先的 Obsidian 插件，将滴答清单的任务、清单、习惯与专注记录，同科研项目阶段、结构化复盘、数据分析及可选挑战系统统一到一个工作台中。

当前阶段仅开发和验证桌面端。暂不发布到 Obsidian 插件社区；后续通过 GitHub Release 和 BRAT 安装维护。

## 不可违反的规范

1. 滴答清单是任务、远端清单、习惯和专注记录的权威数据源；项目与阶段的身份、状态和正文只存于 Markdown，三类阶段关系与布局只存于专用 Canvas；`data.json` 不得保存项目、阶段或关系的影子真值。Helix 负责前端、校验和安全写入。
2. 本地与远端同时修改同一对象时，禁止静默覆盖。必须保留 Base、Local、Remote 三方快照，并由用户逐字段确认。
3. 凭证只能存入 Obsidian SecretStorage。不得写入 `data.json`、日志、审计记录、测试夹具或 Markdown。
4. 用户 Markdown 与 Canvas 内容优先。写入前必须比较读取版本；只修改 `helix-*` 属性、明确的 Helix 管理块、节点身份元数据或三类 Helix 管理边，保留未知 YAML/JSON、正文、布局和非托管边。旧字段和旧边只能在逐项预览并确认后迁移，禁止静默推断。
5. 同步、领域和 UI 必须分层。`src/domain` 与 `src/sync` 不得依赖 Obsidian UI；关键逻辑必须有自动化测试。
6. 离线创建超时属于“远端结果未知”，不得盲目重试。冲突只冻结对应对象，其他对象继续同步。
7. 不得声称滴答 `etag` 提供原子写入，除非真实合同测试证明。默认执行写前复查与写后验证。
8. DidaSync 衍生行为和 Research Card Manager 交互参考必须保留 MIT 归属与上游提交来源。不得复制 GPL 或 FSL 项目的实现代码。
9. 使用标准 Git 工作流。日常开发、修复和验证只在 `dev`；只有用户明确要求“发布新版”时，才允许将 `dev` 合入 `main`、创建标签并发布 Release。每个阶段只有在规定验证通过、文档同步更新后才能提交。
10. 除非核心规范确实变化，否则不要修改本文件。当前状态只维护在 `docs/STATUS.md` 快照中。

## 文档导航

- `docs/ARCHITECTURE.md`：权威边界、模块结构、数据流和关键边界情况。
- `docs/DEVELOPMENT.md`：环境、命令、Git 阶段提交、测试与 BRAT 发布流程。
- `docs/STATUS.md`：当前事实、工作树、验证、风险和下一步的跨对话接手快照；每次工作必须读取。
- `docs/history/`：按日期保存历史状态，仅在追溯回归或历史决策时读取，日常开发不得默认加载。

## 每次工作的固定流程

1. 阅读本文件和 `docs/STATUS.md`；涉及结构或同步时再读 `docs/ARCHITECTURE.md`。
2. 检查工作树，保护用户已有修改。
3. 用最新事实更新 `docs/STATUS.md`；阶段完成后替换旧状态，不追加永久流水账。已提交阶段的详细证据进入 Git 历史，必要时按日期归档到 `docs/history/`。
4. 实现并运行与风险相称的测试。
5. 由项目主管智能体进行独立审查。
6. 修复 P0/P1/P2 问题，更新文档。
7. 通过阶段闸门后形成一个职责单一的 Git 提交。

## 基础验证命令

```bash
npm test
npm run typecheck
npm run build
```

仅有构建和单元测试通过不能证明插件可用。涉及界面的阶段还必须完成真实 Obsidian 桌面端启停、重载、交互和控制台检查。
