# 当前开发状态

最后更新：2026-08-21
当前基线提交：`6f77fdf fix: preserve stage rows across continued growth`（功能基线）
工作树状态：工作树干净。
当前阶段：`dev` 上继续打磨项目关系图；`main` 与正式版仍停留在 `2.1.0`。

## 本轮目标

- 优化推进节点的纵向落位，并在悬停阶段卡片时突出其完整上游生长链路。
- 非目标：不合并 `main`、不发布新版、不启用或访问滴答远端、不修改正式 Vault。

## 当前事实

- Project／Stage Markdown 是项目身份、状态、正文与本地任务的权威源；专用 Canvas 是关系和布局的权威源。
- 日常开发只在 `dev`；只有用户明确要求“发布新版”时才允许合入 `main`、打标签和发布 Release。
- `main` 当前正式版为 `2.1.0`；本轮关系图改进仅存在于 `dev`。
- Stage `# 计划行动`直接进入任务页并形成任意深度本地任务树；带子任务的父任务完成状态由直属子任务派生。
- 项目关系图支持推进、分支、合并、关系中插、项目显式排序、容器／阶段 Shift 吸附、作用域整理及布局保存读取。
- Canvas `helixProjectOrder` 维护自动布局的项目容器顺序；旧库无账本时按当前容器位置推导，新项目追加到末尾。
- 新建阶段保留当前项目内既有阶段的纵向轨道；连续“推进”沿直接父节点 Y 生长，新分支从既有后继下方稳定展开。
- 悬停阶段卡片时，当前卡片、所有反向可达父阶段及路径边保持原样；后继和无关卡片／边弱化，合并会保留全部上游分支。
- 悬停高亮仅是会话投影，不写 Markdown、Canvas、设置或撤销栈。
- 自动同步仍需显式启用；当前测试 Vault 的自动同步为关闭状态。

## 本轮改动

- `src/domain/project-graph.ts` 与 `src/services/project-workspace.ts`：创建阶段时冻结既有纵向轨道，为推进和新增分支分别计算新节点 Y。
- `src/ui/project-lineage-workbench.ts` 与 `styles.css`：增加上游关系反向遍历、边身份标记及卡片／边渐隐状态。
- 相关领域、服务、UI 纯函数和结构合同新增回归测试；架构与设计验收文档同步更新。
- `AGENTS.md` 固化 `dev` 开发、用户明确要求后才发布的分支规则。

## 相关约束

- 权威边界、关系 DAG、布局与会话投影合同见 [ARCHITECTURE.md](ARCHITECTURE.md) 第 5 节。
- 阶段创建不得重排当前项目内既有阶段的 Y；推进同行、分支向下扩展，同时允许后续项目容器为避免重叠而整体下移。
- 悬停高亮必须遍历全部托管入边；不得把后继、无关节点或项目兼容锚点误算为上游链路。
- Canvas 写入继续使用 revision CAS；未知字段、非托管节点及用户 Markdown 正文不得静默改动。
- 凭证只存 SecretStorage；冲突禁止静默覆盖；远端结果未知禁止重发。

## 当前验证

- 完整门禁通过：71 个测试文件、1020 项；`typecheck`、`build`、`release:check`、`git diff --check` 全部通过。
- 自动化覆盖下方分支连续创建两级推进：X 逐级递进、全部既有节点 Y 逐项不变、两级后继与父节点 Y 完全相等；原有分支、合并及项目排序回归通过。
- 自动化覆盖菱形合并 DAG：悬停合并节点保留两个来源分支及共同根，排除后继和无关链路。
- Obsidian CLI 在测试 Vault 连续创建两个临时推进 Stage：父、子、孙 Y 均为 `486`，第二次创建时既有节点移动数为 `0`；临时 Stage 已通过可恢复删除流程清理。
- CLI DOM 验收真实关系图：悬停目标时仅目标、两级父节点与两条上游边保持高亮，离开后所有渐隐类清零。
- 插件错误缓冲和 error 控制台均为空；自动同步为 `false`，未访问滴答或正式 Vault。
- 本轮未取得可信截图对比，不能宣称完成视觉截图验收；交互与 DOM 状态已实机验证。

## 未关闭问题

- 用户尚未在日常画布手动确认渐隐强度是否符合视觉偏好；当前数值为卡片 `0.18`、边 `0.12`。
- 普通滴答子任务新增、删除和排序仍受真实合同能力约束，不属于本轮范围。

## 下一步

1. 用户在测试 Vault 手动体验推进节点同行与多分支祖先高亮，反馈渐隐强度或过渡手感。
2. 后续关系图改进继续只提交到 `dev`，保持 `main` 与 Release 不变。
3. 仅在用户明确要求“发布新版”后，重新跑发布门禁、合并 `main` 并发布。
