# 当前开发状态

最后更新：2026-08-17
当前基线提交：`697c59b docs: snapshot 1.0.2 release baseline`（分支 `main`）
工作树状态：工作树干净。
当前阶段：`1.0.2` 本地项目画布更新已正式发布，不引入 `dev` 的滴答同步提交。

## 本轮目标

- 完成 Project Canvas 对齐、容器整理、布局恢复、关系中插阶段、编号预览与项目颜色编辑。
- 非目标：不合并 `dev`、不开放滴答网络同步或项目投影、不访问滴答远端、不修改正式 Vault、不发布新版本。

## 当前事实

- Project／Stage Markdown 保存身份、状态和正文；专用 Canvas 保存关系及布局；`data.json` 不保存项目影子真值。
- 项目／阶段名称支持关系图与 Markdown 双向更新，写入使用 revision／CAS。
- Stage `# 计划行动` 的顶层清单项自动进入任务页，缩进项作为子任务；首次读取会静默补稳定身份，空模板占位保持原样。
- 本地任务详情采用 Dense task canvas，支持日期月历、无时间／时间点／时间段及子任务增删改排；全部通过 Stage revision/CAS 写回原 Markdown，不建立第二份真值。
- 项目色只用于容器／卡片细边；五状态使用原位菜单及语义图标。
- 正文输入期间暂停派生扫描；失焦后合并刷新，后台 Helix 页面不抢编辑焦点。
- 项目启动等待布局就绪与双稳定快照；结构变化离屏构建后替换，避免闪烁。
- 普通任务与 Helix 项目／阶段的显式关联集中在任务页展示，项目页不再呈现关联任务面板。
- 聚焦桥接以 Canvas 入边为关系源，支持推进、分支、合并、反向编辑及同窗竞争冲突。
- 聚焦引用只保留上下各一空行；Live Preview 隐藏技术标记。
- `helix-sequence` 保持物理文件高水位且不复用；`helix-stage-code` 按当前继承／分支／合并图维护，删除会原子更新现存 Stage 标题、属性、聚焦引用及 Canvas 编号索引。
- 冲突中心分级展示；文本使用真实行号 IDE diff，标量属性按需展开。
- `1.0.2` 继续统一关闭滴答同步与项目投影的界面、命令、调度及服务网络入口，既有口令保留在 SecretStorage。
- 版本 `1.0.2`，最低 Obsidian 桌面端 `1.12.2`；通过 GitHub Release／BRAT 分发。

## 本轮改动

- Shift 拖动阶段卡片可按同轴位置吸附；关系图阶段卡片继续使用统一紧凑尺寸。
- 局部整理把其他项目完整容器作为碰撞单元，避免卡片未相交但容器重叠。
- 工具栏新增“读取保存布局”，可放弃未保存视图变化并恢复 Canvas 最后存档。
- 关系箭头不显示文字标签，依靠颜色表达类型；宽命中层不改变视觉线宽，悬停或聚焦时仅在当前箭头显示灰白插入加号。
- 创建阶段弹窗显示真实 `helix-stage-code` 预览，分支显示 `2.1/2.2` 而非物理文件序号。
- 项目铅笔入口改为编辑窗口，可同时修改项目名称与颜色。
- 分支边中插时，新节点接管原目标的小编号槽位；原目标及后继按新 DAG 顺延。

## 相关约束

- 权威源、Stage 本地任务及项目投影边界见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 项目写入必须串行并使用 revision／CAS；不得覆盖未知正文或保存第二份本地任务真值。
- 本地子任务重排只允许同一父任务的直接子任务身份全集；身份缺失、重复或集合变化必须拒绝。
- 远端结果未知时禁止重试；同字段竞争必须逐字段人工选边。
- 真实滴答测试只可使用唯一合同标记和专用清单；不得删除、移动或覆盖用户已有远端对象。

## 当前验证

- 最新完整门禁：69 个测试文件、981 项测试，以及 `typecheck`、`build`、`release:check`、`git diff --check` 全部通过。
- 既有实机验证：Stage 可持续编辑，项目聚焦和创建阶段不闪烁，本地项目主流程可用。
- 2026-08-17 CLI 实机验收：分支弹窗显示 `阶段 2.2`；关系中插后形成连续推进；在真实 `1→2.1` 分支边中插时新节点保持 `2.1`、原目标顺延，删除临时节点后原边和编号恢复；读取保存布局与项目颜色控件可用；悬停加号修改重载后错误缓冲为空。
- 发布安全扫描未发现凭证或本机绝对路径；本轮未访问滴答远端，测试 Vault `autoSync=false` 且口令保留。
- GitHub Release `1.0.2` 已发布，标签指向 `697c59b`；BRAT 三项运行产物及第三方声明、许可证附件均已上传并核验。

## 未关闭问题

- 本地任务暂不支持跨 Stage／项目移动；需要多文件事务后再开放。
- 滴答 `items` 写入结果未知，项目滴答投影继续关闭。
- 2026-08-05 孤儿合同任务远端状态仍不确定，禁止盲目清理。

## 下一步

1. 等待 `1.0.2` 真实使用反馈，继续以向后兼容方式修复本地体验。
2. 滴答能力继续留在 `dev`，用户确认前不合并。
