# 当前开发状态

最后更新：2026-08-13
当前基线提交：`db90619 fix: prevent sparse projection false conflicts`（分支 `dev`）
工作树状态：基线代码已提交；仅本状态快照待提交。
当前阶段：v13 第六次合同已安全中止并清理；已修复稀疏 Local 快照假冲突与合法账本重启误报，等待后续最终复验。

## 本轮目标

- 不触碰用户既有滴答对象和正式 Vault，验证 Project／Stage 到滴答父子任务链路。
- 非目标：不发布、不启用生产同步、不替用户选清单／分栏、不以真实请求触发限流。

## 当前事实

- Project／Stage Markdown 是项目内容权威源，专用 Canvas 保存关系与布局；`data.json` 只保存恢复和同步状态。
- Stage `# 计划行动` 顶层条目进入任务页，缩进条目为本地子任务。
- 滴答能力按只读、普通写入、专用合同、项目投影分层；默认关闭，启用须版本化二次确认。
- 普通任务 CRUD、移动、完成／重开、冲突和父子关系已有 v12 合同证据。
- Local 与 Remote 相同时零写入；中断写入重启后冻结，不得重发。
- 连接使用 unknown／online／offline 三态；仅 transient 读取失败进入 offline，离线编辑只排队，成功同步后再消费。
- 离线可保存冲突选择，但应用在远端读取前拒绝。
- 离线时未知写入与冲突采纳冻结；跨清单移动仅在源或目标唯一可见时恢复。
- 限流状态持久化；冲突中心显示截止时间，到期后只允许一次受控读取，成功前不消费写队列。
- 项目映射为稳定 marker 父 Task；Stage 行动映射为带 `parentId` 和 marker 的子 Task，不使用 `items`。
- 项目删除精确排队删除远端子任务及父任务；tombstone 阻止重发和复活。
- 结果未知禁止重发；字段竞争保留三方快照，必须人工处理。
- 版本 `1.0.1`，最低 Obsidian `1.12.2`；当前不发布。

## 本轮改动

- 第六次真实合同已推进到 Vault Project／Stage 父子任务创建、远端 ID 回填和标题更新；未出现 `markdown-race` 或时区误判。
- 失败点为稀疏投影 Local 快照遗漏未写字段，三方比较误判为本地清空并制造假冲突；现改为 Base 补齐，显式 `writeFields` 仍可清空字段。
- 持久投影账本校验器已接受服务自身写入的 `remoteEntity`，重启不再把合法状态误报为损坏。
- 合同失败路径在所有远端 ID 已登记后自动清理临时队列、冲突、收据、事件、投影状态及 Project／Stage／Canvas；身份不完整时继续冻结。
- 本轮唯一 run ID `2f46b657-f51a-4f85-b111-c843d9c94269` 的远端与本地测试对象已精确清理，未触碰既有对象。

## 相关约束

- 权威源、能力缓存、合同清理、请求治理和项目投影规则见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 真实测试只创建唯一标记对象；删除前核对 ID、marker 和归属，结果未知时只读复核、绝不重发。
- 凭证只存 SecretStorage；正式 Vault 和用户既有滴答任务、清单、习惯、专注及配置均不在测试授权范围。

## 当前验证

- 最新完整门禁：70 个测试文件、991 项全部通过；`typecheck`、`build`、`release:check`、`git diff --check` 通过。
- v12 真实合同已通过：194 次主体调用，生产队列创建／更新／完成／重开／删除闭环成立；临时对象和恢复状态最终归零，未触发限流。
- v13 六次均安全中止；本轮假冲突、重启校验和失败自清洁均已有针对性回归，但完整 v13 仍未通过。
- 第六次未触发限流；专用远端对象和本地合同项目已清理，队列、冲突、恢复、投影账本、收据、待清理计划及相关任务快照均为零。
- 测试 Vault 保持原有两个本地项目，临时合同项目已移除；项目投影禁用且账本为空。旧滴答展示缓存已通过纯本地入口清空，口令和事件账本保留。
- CLI 重载后清理计划、队列、冲突、恢复、投影和收据均为零，`autoSync=false`；禁用态零后台联网。
- 2026-08-13 只读拉取成功：6 清单、72 任务、1 专注；队列、冲突、恢复和限流均为零。习惯接口不可用但保留旧缓存；随后清空本地展示缓存。
- v13 不得宣称通过；本轮不得再次执行真实合同，最新三项修复仅有自动化门禁与零残留实机证据。

## 未关闭问题

- 429／查询限流已有故障注入和持久治理；不得以真实限流验证。
- 本地任务暂不支持跨 Stage／项目移动；提醒、重复和检查项能力保持只读。

## 下一步

1. 后续再次获得明确授权后，仅运行一次 v13 最终合同，并核对专用远端对象与本地恢复状态全部归零。
2. 合同通过后在测试 Vault 激活唯一专用目标，验收父子任务后台投影、通知、冲突冻结和安全删除。
3. 更新发布证据并形成 dev 合并候选；本阶段不发布。
