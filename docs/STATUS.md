# 当前开发状态

最后更新：2026-08-09
当前基线提交：`ed24512 feat: refresh managed review summaries on open`
工作树状态：工作树干净（本状态快照提交后）。
当前阶段：`0.1.5` 个人预览发布就绪。

## 本轮目标

- 固化 Canvas 安全缓存静默修复，并让既有复盘在打开时更新受管统计摘要。
- 非目标：不重试 items 真实合同，不开放项目投影，不连接或修改用户滴答及正式 Vault。

## 当前事实

- 任务、清单、习惯和专注记录以滴答为权威源；任务同步、列表／看板和专注记录读取已有实现，当前可继续使用滴答 App 管理任务。
- Project／Stage Markdown 保存项目身份、状态和正文，专用 Canvas 保存继承／分支／合并关系及布局；`data.json` 不保存项目影子真值。
- 项目投影底层采用“项目父任务 + 阶段行动 `items`”；整组更新会重生成 item ID，只有成功响应后的完整唯一映射才能原子更新。
- Base／Local／Remote 同字段竞争必须逐字段人工解决；凭证只存 SecretStorage，所有远端写入复用唯一队列和 `RemoteWriteGate`。
- 当前版本不展示项目投影入口；统一门禁覆盖后台扫描、队列、恢复、重试和冲突写回，历史操作只读保留且不阻塞普通任务。
- Project／Stage 扫描仅解析含顶层 `helix-kind` 的 frontmatter；普通正文与聚焦块不解析、不改写，Helix 元数据仍严格校验。
- Helix 自写 Project／Stage／Canvas 时合并同步与延迟 watcher 事件，quiet-window 后只触发一次稳定重扫；外部事件仍监听，失败也不吞事件。
- Canvas 摘要与阶段编号账本是可重建缓存，可在稳定 CAS 后静默修复；缺失节点、关系变化和迁移仍需人工确认。
- 既有复盘每次打开会重算明确 marker 内的统计；缺失/重复 marker 时不改正文，模板只用于首次创建。
- 版本兼容基线为 Obsidian桌面端 `1.12.2`；版本文件均已登记 `0.1.5`。

## 本轮改动

- `src/domain/journals.ts`、`src/main.ts`：既有日/周/月/年复盘打开时通过 CAS 更新受管摘要块，其他内容不变。
- 测试覆盖幂等替换、歧义 marker 拒绝、既有正文与关闭状态保护；版本文件更新至 `0.1.5`。

## 相关约束

- 权威源、项目投影、全组 ID 重映和未知结果处理见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 真实测试只能使用唯一合同标记和专用测试清单；不得删除、移动或覆盖用户已有远端对象。
- 合同非幂等写只发送一次；结果未知时不得重试或用非唯一结果推断成功。
- 2026-08-05 遗留的孤儿合同任务：删除与后续复读均返回 Dida 500。不得宣称已经清理，也不得盲目重试；只能在明确授权、可识别身份和安全预算下另行核对。

## 当前验证

- v0.1.5 完整测试 67 个文件、934 项及 `typecheck`、`build`、`release:check`、`git diff --check` 通过；真实测试 Vault 中 Canvas 过期摘要后台恢复且前后字节一致。
- 真实项目回归通过创建、2.1/2.2 分支、阶段 3 合并、状态更新、中间分支删除桥接、撤销/重做与插件重载；最终无修复项、恢复问题或关系丢失。
- 真实复盘回归通过四周期创建、重复打开字节不变、关闭只改属性，以及重新打开仅刷新自动摘要并保留用户正文。
- 最终 30 秒真实合同仍在 items 写入时超时并进入结果未知；后续复读无法唯一证明写入结果。全程零重发，安全清理完成：`cleanup clean`、`remoteArtifacts=false`。
- 当前 `pending=0`、`queue=0`、`conflict=0`、`recovery=0`，且未观察到限流。
- 上述结果是外部真实环境阻塞，不是 items 合同通过；不得宣称生产项目投影可用。当前干净状态也不等于 2026-08-05 孤儿任务已清理。

## 未关闭问题

- 滴答 items 写入在 30 秒内仍不返回，且结果未知后的复读不唯一；真实合同与真实项目投影维持阻塞。
- 2026-08-05 孤儿合同任务的远端状态仍不确定，当前无安全自动清理结论。
- 项目与复盘体验仍是正式版前的核心开发范围。
- 项目投影底层保留但发布门禁关闭；未来重新开放必须先解决 items 合同阻塞，并新增真实安全合同与迁移验收。

## 下一步

1. 验证任务／项目引用及复盘统计闭环，修复正式使用阻塞。
2. 补齐项目与复盘的高频实机体验并保持向后兼容。
3. 保持项目滴答投影关闭，待外部合同稳定后再独立解锁。
