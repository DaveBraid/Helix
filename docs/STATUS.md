# 当前开发状态

最后更新：2026-08-21
当前基线提交：`4156a24 docs: snapshot Helix 2.1.0 release baseline`
工作树状态：工作树干净。
当前阶段：Helix `2.1.0` 已在 GitHub 正式发布；`main` 与 `dev` 已同步。

## 本轮目标

- 发布包含项目排序、Shift 对齐与新增 Stage 顺序修复的 Helix `2.1.0`。
- 非目标：不启用或测试滴答远端、不修改正式 Vault 数据、不发布到 Obsidian 社区目录。

## 当前事实

- Project／Stage Markdown 是项目身份、状态、正文与本地任务的权威源；专用 Canvas 是关系和布局的权威源。
- `2.1.0` 是当前正式版，标签指向已验证的发布基线 `4156a24`；GitHub Release 非草稿、非预发布。
- Stage `# 计划行动`直接进入任务页并形成任意深度本地任务树；编辑使用 Stage revision CAS，不在 `data.json` 保存影子任务。
- 有子任务的本地行动与 Stage 主任务完成状态为派生值：全部直属子任务完成后自动完成，任一子任务重新打开后自动恢复未完成。
- 项目关系图支持推进、分支、合并、关系中插、容器级整理、布局保存读取、项目／阶段改名和颜色编辑。
- 关系图完整自动布局现在以调用方项目顺序作为容器 lane 顺序；Canvas `helixProjectOrder` 是该显式顺序的内部账本。
- 无排序账本的旧 Canvas 按当前阶段容器的上、左位置推导兼容顺序；账本缺少的新项目追加，已删除的旧 ID 忽略。
- 关系图 Shift 吸附独立处理 X、Y 轴与等间距候选；阶段只使用其他可见结构阶段，项目容器只使用其他项目容器，禁止跨层级吸附。
- 新增 Stage 的局部整理只避让排序位于当前项目之前的容器，并在结束时按账本校正全部容器顺序；靠后的项目可下移，当前项目不会被推到队尾。
- 自动同步仍需显式启用；当前测试 Vault 的自动同步为关闭状态。

## 本轮改动

- `main` 已快进合入 `dev` 上 2.0.0 之后的六个项目关系图提交，无冲突。
- `manifest.json`、`package.json`、`package-lock.json` 与 `versions.json` 已统一为 `2.1.0`，最低 Obsidian 仍为 `1.12.2`。
- `README.md` 与 `docs/releases/2.1.0.md` 已补充升级说明、数据边界、已知限制和许可证说明。
- `main`、`dev` 与标签已推送；Release 包含 BRAT 三个运行文件、第三方声明和四份许可证／通知附件。

## 相关约束

- 权威边界、Canvas 排序账本、布局与拖动合同见 [ARCHITECTURE.md](ARCHITECTURE.md) 第 5 节。
- 项目排序必须是当前全部项目的无重复排列；写入绑定打开时 Canvas revision，竞争时零写入拒绝。
- 排序只写内部账本和托管节点坐标，未知 Canvas 字段、非托管节点、Markdown 正文不得改动。
- 对齐引导是纯会话投影；只有用户明确保存当前布局时才写 Canvas。
- 凭证只存 SecretStorage；冲突禁止静默覆盖；远端结果未知禁止重发。

## 当前验证

- 完整门禁通过：71 个测试文件、1017 项；`typecheck`、`build`、`release:check`、`git diff --check` 全部通过。
- 领域与服务合同覆盖局部项目增长和真实新增 Stage 后的显式顺序保持；原有新增／分支／合并布局回归全部通过。
- Obsidian CLI 在测试 Vault 首项目创建临时 Stage：账本始终为“项目 2 → 测试项目”，容器顺序未反转；临时 Stage 随后通过可恢复删除流程清理。
- CLI 已重载 `2.1.0` 发布构建；插件启用，错误缓冲和 error 控制台均为空；自动同步仍为 `false`，未访问滴答或正式 Vault。
- GitHub Release 的 8 个附件均为 uploaded；远端 SHA-256 与本地发布文件逐项一致。

## 未关闭问题

- 多项目真实使用时仍可观察新增复杂分支导致后续容器下移的间距是否符合手感；当前合同与实机路径均已通过，不构成阻塞。
- 普通滴答子任务新增、删除和排序仍受真实合同能力约束，不属于本轮范围。

## 下一步

1. 用户可通过 BRAT 或 GitHub Release 更新到 `2.1.0`，重点体验多项目排序、Shift 对齐和新增 Stage 后的容器顺序。
2. 后续项目关系图反馈继续在 `dev` 修复，稳定后再安排版本更新。
3. 后续滴答开发继续遵守专用临时对象与显式远端写门边界。
