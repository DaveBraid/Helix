# 当前开发状态

最后更新：2026-08-05
当前基线提交：`0a4e38c feat: model safe Dida project projection`
工作树状态：阶段 4A 共享同步接入尚未提交；Vault 测试数据与运行态 `data.json` 不纳入 Git。
当前阶段：阶段 3D 已提交，等待 CLI 实机验收；阶段 4A 自动化与审查已通过。

## 本轮目标

- 完成阶段聚焦桥监听、持久冲突和恢复事务，并形成阶段提交。
- 随后把 Helix 项目／计划行动单向投影接入既有滴答队列；本轮不发布 Release，不触碰既有滴答对象。

## 当前事实

- 滴答清单是任务、清单、习惯和专注记录的权威源；Project/Stage Markdown 保存项目身份、状态与正文，专用 Canvas 只保存阶段关系和布局，见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 滴答写入合同版本为 5；旧合同、授权不符、结果未知或身份不明均冻结为只读。新增重开能力只在专用临时任务中验证最小 `status=0` 载荷。
- 工作台导航为今日、项目、任务、复盘、挑战、冲突；侧栏固定，主内容独立滚动；复盘已合并原分析页。
- 六份默认模板位于可配置目录 `Template/Helix/`；只补缺失文件，不覆盖现有模板。
- 项目状态为计划中、进行中、完成、暂停、终止；阶段状态为想法、进行中、完成、暂停、终止。旧 `archived/planned/closed` 仅兼容读取，不批量回写。
- 阶段编号写入 `helix-stage-code`：分支使用 `N.1/N.2`，继承与合并推进大号，历史编号不复用；继承转分支是唯一受控改码例外并同步受管 H1。
- 活动 Project/Stage 笔记通过公开状态栏和命令编辑状态；项目页状态、关系、布局、折叠、颜色、撤销/重做及修复均经过恢复模式写入闸门。
- 项目页纯读取不写 Canvas；显式修复使用稳定双快照、Markdown 复核和 Canvas CAS。
- 阶段看板从 Markdown 派生卡片；状态迁移只写对应 Stage Markdown。
- 阶段聚焦桥通过非权威 Base 检查点协调来源／派生编辑；同源多目标反写先归并候选，竞争只冻结相关 pair。
- 自动聚焦同步不进入用户撤销栈；冲突解决使用可恢复 `pendingResolution`，未知混合态冻结整个项目工作区。
- 项目投影父任务／行动的写入统一经过现有授权租约、离线队列、同步引擎和唯一消费者；稳定 client identity 在远端发送前持久化，中断创建不重发。
- 投影操作收据不保存完整任务；`verified` 只能从清单、任务和标记均匹配的 Base 快照恢复。投影默认关闭。

## 本轮改动

- 阶段 3D 已提交为 `c8f4411`。
- 阶段 4 独立层已提交为 `0a4e38c`；4A 共享队列、重开合同、严格持久状态及测试尚未提交。
- Vault 旧项目测试文件及旧 Canvas 已通过 Obsidian CLI 移入废纸篓；新建 2 项目、6 阶段的分支／合并／五状态隔离夹具（不纳入 Git）。

## 相关约束

- 凭证仅存 SecretStorage；不得进入仓库、`data.json`、日志、Markdown、Notice 或夹具。
- 用户 Markdown/Canvas 优先；只修改明确受管字段、块、节点和边，写前必须复核修订。冲突不得静默覆盖。
- 恢复模式下项目页可读，但所有写入口必须拒绝；纯读取不得顺手修复派生数据。
- 真实滴答测试仅限唯一标记的专用对象；未知结果、残留或清理身份不明立即停止。
- `ObDevTestVault` 内数据已获用户授权，可直接修改用于插件测试；该授权不扩展到用户真实滴答数据。

## 当前验证

- 已通过：全量 59 个测试文件、706 项，`typecheck`、`build`、`release:check`、`git diff --check` 全绿。
- 阶段 3D：定向 178 项通过；看门狗最终复审 P0/P1/P2/P3=0，可提交。
- 阶段 4A：定向 211 项通过；看门狗最终复审 P0/P1/P2=0，可提交。未执行真实滴答写入。
- 实机（提交基线）：Obsidian 1.13.4 深色模式五列横排／独立横滚正常；隔离 Stage 跨列写入与计数一致，同列拖拽零写，键盘和 ARIA 正常，`dev:errors` 为 0。
- 本轮实机：尚未验证。Obsidian 当前未运行，桌面启动通道因用量限制被拒；不得宣称 3E 通过。

## 未关闭问题

- 阶段 3E 的 Live Preview、隐藏标记、正反向编辑、冲突 UI 和控制台检查尚未实机验证。
- 阶段 4 尚缺设置／项目页／冲突中心入口、缺列显式创建及真实合同；当前仍不能对用户开放。
- 真正首次安装的模板目录弹窗仅有自动化覆盖；未伪造当前安装状态。

## 下一步

1. 提交阶段 4A，并在 Obsidian 可运行后完成阶段 3E CLI 实机验收。
2. 补阶段 4B 只读模型、行动纳管／编辑、设置精确预览双确认、单项目同步和冲突中心入口。
3. 在专用临时清单完成投影与重开真实合同；未知结果零重发并精确清理。
4. 合同与审查通过后才允许显式创建／选择“Helix项目”分栏；不得同步当前测试夹具。
