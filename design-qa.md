# 项目阶段卡片与本地任务详情视觉验收

## 证据

- 项目源图：`/var/folders/vv/5ssln5h12vz4y1b8gbp2x05c0000gn/T/codex-clipboard-e779cdf1-6314-4ace-998b-8e161e1f6f29.png`
- 项目实现图：`/private/tmp/helix-stage-cards-final-hover.png`
- 项目全景并排：`/private/tmp/helix-project-design-compare.png`
- 任务源图：`/var/folders/vv/5ssln5h12vz4y1b8gbp2x05c0000gn/T/codex-clipboard-cbc899b9-6218-4266-8788-b4536738d4cf.png`
- 任务实现图：`/private/tmp/helix-task-dense-v1.png`
- 任务全景并排：`/private/tmp/helix-task-design-compare.png`
- viewport：Obsidian 1.13.4 桌面端；实现截图 2790 × 1846 px，device scale factor 约 2；源图分别为 1828 × 860、1586 × 992 px。
- normalization：两组全景均保持纵横比缩放到 900 px 高后左右并排，没有拉伸。
- state：项目图使用测试 Vault 的两个单阶段项目，首卡处于操作栏显示状态；任务详情使用两条本地子任务，属性矩阵与无备注状态完整呈现。

## Findings

- 无未关闭 P0/P1/P2。
- [P3] 项目源图展示完整分支／合并关系，测试数据只有单阶段项目，因此截图不能复现相同关系密度；关系线代码和交互未在本轮改动，卡片尺寸、状态、项目色和操作栏可独立对照。
- [P3] 项目源图为浅色概念稿，实现截图为当前测试 Vault 深色主题；所有新增色彩均使用 Obsidian 主题变量或低透明度语义色，未引入固定大面积填充。
- [P3] 任务实现截图为 0/2，源图为 2/4；实机 AX/DOM 另验收 1/2 与 50% 进度、09:30–16:00 时间段，差异来自测试内容而非布局能力。

## 比较历史

1. P1：阶段卡片原为项目渐变填充，并把新增、连接、删除拆成深色悬浮圆按钮。已改为中性主体、统一项目色顶部细条／边框／轻阴影，并将新增、连接、折叠、删除收进同尺寸操作栏。
2. P1：旧任务编辑器包含备注、时区和两个 `datetime-local`，缺少设计图的属性矩阵与进度主体。已删除可见备注和时区，改成状态／优先级／日期／自适应时间／标签矩阵，并保留底层时区换算。
3. P1：子任务只能改标题、完成或删除，不能持久化排序及可选日程／优先级。已增加拖拽手柄、更多菜单、日期、时间段、优先级和 Markdown 顺序持久化。
4. P2：首版卡片标题受到当前主题按钮样式污染，出现厚重按钮底色和居中。已用 Helix 根作用域强制透明、左对齐，并把关系摘要与操作栏做等位淡入淡出。
5. P2：子任务更多菜单出现浏览器默认开合三角。已隐藏默认 marker，仅保留 Obsidian `ellipsis` 图标。

## 验收面

- 字体与排版：任务正文基础字号 14 px，标题 24–30 px；阶段标题 14 px，状态与关系 10–11 px；无微小关键信息。
- 间距与布局：任务模态 960 px 上限，两行五格属性矩阵，进度环与列表双栏；窄屏降为两列属性和单列子任务。
- 色彩与令牌：同项目卡片只使用项目主题色；五状态仅以图标、文字和语义色区分；已完成反馈最强。
- 图像与资产：无位图依赖；图标全部复用 Obsidian 图标库。进度由实时子任务数据渲染。
- 文案与行为：任务详情没有备注区和时区文字；时间支持无时间、时间点、时间段；操作栏不改变卡片尺寸。
- 交互：状态选择、日期／时间切换、子任务完成、添加、更多菜单、拖拽重排、删除确认与保存路径均保留；未访问滴答远端。

final result: passed
