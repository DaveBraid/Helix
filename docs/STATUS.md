# 当前开发状态

最后更新：2026-08-14
当前基线提交：`f91d655 fix: stabilize background Dida project projection`（分支 `dev`）
工作树状态：插件代码仅有本状态快照待提交；测试 Vault 的 Stage-05 含验收改动。
当前阶段：v13 专用目标后台投影主路径与完整门禁已通过；进入字段竞争验收。

## 本轮目标

- 在唯一专用清单验证 Project／Stage 后台父子任务投影、重启／禁用门禁与恢复收口。
- 非目标：不发布、不触碰用户既有滴答对象或正式 Vault、不以真实请求触发限流。

## 当前事实

- Project／Stage Markdown 是项目内容权威源，专用 Canvas 保存关系与布局；`data.json` 只保存恢复和同步状态。
- Stage `# 计划行动` 顶层条目进入任务页，缩进条目为本地子任务。
- 滴答能力按只读、普通写入、专用合同、项目投影分层；项目投影启用须 fresh 预览、版本化二次确认和运行态 readiness。
- 普通任务 CRUD、移动、完成／重开、冲突和父子关系已有 v12 合同证据。
- Local 与 Remote 相同时零写入；中断写入重启后冻结，不得重发。
- 连接使用 unknown／online／offline 三态；仅 transient 读取失败进入 offline，离线编辑只排队，成功同步后再消费。
- 离线可保存冲突选择，但应用在远端读取前拒绝。
- 离线时未知写入与冲突采纳冻结；跨清单移动仅在源或目标唯一可见时恢复。
- 限流状态持久化；冲突中心显示截止时间，到期后只允许一次受控读取，成功前不消费写队列。
- 项目映射为稳定 marker 父 Task；Stage 行动映射为带 `parentId` 和 marker 的子 Task，不使用 `items`。
- 项目删除精确排队删除远端子任务及父任务；开放任务集合证明不存在，tombstone 阻止重发和复活。
- 结果未知禁止重发；字段竞争保留三方快照，必须人工处理。
- 版本 `1.0.1`，最低 Obsidian `1.12.2`；当前不发布。

## 本轮改动

- 修复激活排他租约内再次申请共享 catalog 租约的自锁；fresh 预览与确认仍在同一排他事务。
- 日程能力在持久排队前拒绝，确定不兼容时零队列、零远端写；修正后可自然重扫。
- 投影状态加载接受完整行动属性；结果未知领养补回已确认目标分栏，并按时间瞬间和标签无序／大小写归一化核对。
- 冻结收口复用已证明分栏身份，远端 ID 成功写回 Stage，队列、收据和冻结全部解除。
- 项目页纳管／编辑行动后按项目失效协调器指纹，确保 Markdown 保存会自动触发后台投影。

## 相关约束

- 权威源、能力缓存、合同清理、请求治理和项目投影规则见 [ARCHITECTURE.md](ARCHITECTURE.md)。
- 真实测试只创建唯一标记对象；删除前核对 ID、marker 和归属，结果未知时只读复核、绝不重发。
- 凭证只存 SecretStorage；正式 Vault 和用户既有滴答任务、清单、习惯、专注及配置均不在测试授权范围。

## 当前验证

- 最新完整门禁：70 个测试文件、996 项全部通过；`typecheck`、`build`、`release:check`、`git diff --check` 通过。
- v12 真实合同已通过：194 次主体调用，生产队列创建／更新／完成／重开／删除闭环成立；临时对象和恢复状态最终归零，未触发限流。
- v13 最终真实合同通过：任务 CRUD、父子任务、项目投影、看板归栏、分栏创建与任务重开均已验证；账号时间语义为 `point`。
- 提醒、重复与检查项能力仍为只读；本轮未触发限流，远端临时对象和本地合同项目均已清理。
- 2026-08-14 专用清单 `[Helix 投影验收 20260814-A]`（唯一 ID 已持久配置）完成后台父任务／子任务创建、改名、完成、重开和删除；未触发限流。
- 真实结果未知对象按唯一远端 ID、marker、清单、父任务、分栏和全部属性领养成功；Stage 回填后队列、收据、冻结、冲突和恢复均归零。
- CLI 重载后 `connected=false` 时零写入；显式禁用后本地改名未触及远端且队列为空；fresh 重新启用后差异自动补齐。
- 最终重载后队列、未决冲突、恢复、收据和收据清理均为零；投影保持启用且运行态未连接时零后台写入。
- 专用清单、两个测试父任务及 7 条行动保留用于后续隔离测试；投影已启用，不得宣称已清理或可发布。

## 未关闭问题

- 专用投影目标仍启用并含测试对象；发布前必须按精确 marker 清理、关闭投影并证明零残留。
- 429／查询限流已有故障注入和持久治理；不得以真实限流验证。提醒、重复和检查项能力保持只读。

## 下一步

1. 在专用对象制造字段竞争，实测逐字段冲突冻结和应用。
2. 验收项目级安全删除；发布前关闭投影并精确删除专用清单。
3. 复核普通任务双向同步和本轮兼容迁移，再决定正式版候选范围。
